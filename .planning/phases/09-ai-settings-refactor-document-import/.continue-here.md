---
phase: 09-ai-settings-refactor-document-import
plan: superpowers (docs/superpowers/plans/2026-04-21-ai-generate-question-redesign.md)
task: 3
total_tasks: 9
status: in_progress
branch: feature/ai-3mode-redesign
worktree: .worktrees/feature/ai-3mode-redesign
last_updated: 2026-04-23T07:38:08.435Z
---

<current_state>
Task 3 (AiSettingsDrawer) widget đã được tạo và commit nhưng code quality review trả về "Needs Work".
Cần fix 2 lỗi important + 5 lỗi minor rồi re-review trước khi mark Task 3 done và chuyển sang Task 4.

File cần fix: `lib/presentation/views/assignment/teacher/widgets/ai_settings_drawer.dart`
</current_state>

<completed_work>

- Task 1: ProcessingMode enum — rename `generation→promptOnly`, add `ragGeneration` — **Done** (commits: 972d8bf, 318d1f5, e747dcc)
- Task 2: `deleteFile()` qua data layer (datasource → repository_impl → notifier + tests) — **Done** (commits: d7a1720, 3bd969e)
- Task 3: AiSettingsDrawer widget tạo xong (commits: 4f504d2, 8e71815), nhưng code quality "Needs Work":
  - Spec compliant (spec reviewer approved)
  - Code quality: 2 important + 5 minor issues chưa fix
</completed_work>

<remaining_work>

- **Task 3 — fix code quality issues:**
  - IMPORTANT #1: `_DocumentLibrarySection` và `_FileListTile` là `StatelessWidget` nhưng có `WidgetRef ref` trong constructor → vi phạm widget lifecycle. Fix: chuyển cả 2 thành `ConsumerWidget`.
  - IMPORTANT #2: `_pickAndUploadFile()` trong `_DocumentLibrarySection` — `file.extension == 'docx'` không có fallback cho extension khác → silent wrong MIME. Fix: guard `'docx' | 'xlsx'`, throw `StateError` nếu khác.
  - MINOR: 3 redundant ternaries tại dòng 101, 153, 172 — cả 2 nhánh đều return `DesignColors.textSecondary`, bỏ ternary.
  - MINOR: Dòng 95 `Colors.white` → `DesignColors.white`
  - MINOR: `debugPrint` trong `export_template_bottom_sheet.dart` → `AppLogger.info`
  - Sau khi fix: commit → dispatch code quality re-review → nếu approved → mark Task 3 done

- **Task 4:** Main screen — thêm `GlobalKey<ScaffoldState> _scaffoldKey`, `endDrawer: const AiSettingsDrawer()`, đổi header button icon `settings_rounded` → `menu_rounded`, `onPressed: () => _scaffoldKey.currentState?.openEndDrawer()`. Xóa nút settings thứ 2 (duplicate). File: `teacher_ai_generate_question_screen.dart`.

- **Task 5:** Mode tabs — thêm `_buildModeTabsSection()` với `SegmentedButton<ProcessingMode>` 3 segments (✏️ Nhập Prompt / 📥 Trích xuất / 📚 Từ Tài liệu). Insert sau header trong Column body.

- **Task 6:** Context-aware body — `_buildModeBody()` switch theo mode: Mode 1 như cũ, Mode 2 hint card + ContextSourcesSection, Mode 3 hint card + ContextSourcesSection + focus hint.

- **Task 7:** Validation fix — read mode đầu `_handleGenerate()`. `ragGeneration` → SnackBar "coming soon". Topic validation chỉ khi `promptOnly`. Mode 2&3 validate `selectedFileIds.isNotEmpty`.

- **Task 8:** Auto-delete — trong `_pollForDocumentResults()` sau `status == 'completed'` gọi `deleteFile(fileId)` + `ref.invalidate(teacherFilesProvider)`.

- **Task 9:** Simplify `AiQuestionSettingsScreen` — xóa `_buildAdvancedSection()` (mode toggle + ContextSourcesSection đã chuyển sang màn chính).
</remaining_work>

<decisions_made>

- Dùng git worktree `feature/ai-3mode-redesign` (`.worktrees/feature/ai-3mode-redesign`) để isolate
- Dark mode surface colors `0xFF1A2632` / `0xFF243040` giữ raw hex — không có DesignColors token
- Mode 3 button show SnackBar "coming soon" thay vì disabled (spec section 11)
- `ContextSourcesSection` giữ nguyên không thay đổi, chỉ wire inline vào main screen body
- WSL2 dart wrapper tại `/mnt/e/Flutter_SDK/flutter/bin/cache/dart-sdk/bin/dart` — bắt buộc để `flutter test` hoạt động
- Superpowers plan (9 tasks) TÁCH BIỆT với GSD phase 09 plans (09-00 đến 09-07) đã done từ trước
</decisions_made>

<blockers>
Không có blockers.
</blockers>

<context>
Đây là redesign 3-mode cho `TeacherAiGenerateQuestionScreen`. GSD phase 09 plans đã hoàn thành trước. Superpowers plan này là tầng redesign UI/UX.

Workflow: Subagent-Driven Development — implementer → spec reviewer → code quality reviewer per task.

Spec: `docs/superpowers/specs/2026-04-21-ai-generate-question-redesign.md`
Plan: `docs/superpowers/plans/2026-04-21-ai-generate-question-redesign.md`

Files quan trọng:
- `lib/presentation/views/assignment/teacher/teacher_ai_generate_question_screen.dart`
- `lib/presentation/views/assignment/teacher/widgets/ai_settings_drawer.dart`
- `lib/presentation/views/assignment/teacher/widgets/context_sources_section.dart`
- `lib/presentation/providers/ai_generation_settings_notifier.dart`
</context>

<next_action>
Dispatch fix subagent cho Task 3 code quality. Fix file `ai_settings_drawer.dart` trong worktree `.worktrees/feature/ai-3mode-redesign`:
1. Convert `_DocumentLibrarySection` → `ConsumerWidget`
2. Convert `_FileListTile` → `ConsumerWidget`
3. Thêm MIME type guard trong `_pickAndUploadFile()`
4. Xóa 3 redundant ternaries dòng 101, 153, 172
5. Dòng 95: `Colors.white` → `DesignColors.white`
6. `export_template_bottom_sheet.dart`: `debugPrint` → `AppLogger.info`

Commit → code quality re-review → Task 3 approved → Task 4.
</next_action>
