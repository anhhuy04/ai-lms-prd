---
phase: 09-ai-settings-refactor-document-import
plan: "03"
type: execute
wave: 2
depends_on: ["09-00"]
files_modified:
  - lib/core/routes/route_constants.dart
  - lib/core/routes/app_router.dart
  - lib/presentation/views/settings/ai_question_settings_screen.dart
  - lib/presentation/views/assignment/teacher/teacher_ai_generate_question_screen.dart
autonomous: true
requirements:
  - "9-01"

must_haves:
  truths:
    - "Gear icon in TeacherAiGenerateQuestionScreen navigates to AiQuestionSettingsScreen"
    - "AiQuestionSettingsScreen has API Key Setup tile that pushes to ApiKeySetupScreen"
    - "AiQuestionSettingsScreen has Thư viện Tài liệu section (empty state initially)"
    - "AiQuestionSettingsScreen has [Xuất file mẫu Excel] button (D-16-ext)"
    - "`flutter analyze` passes with no errors"
  artifacts:
    - path: "lib/core/routes/route_constants.dart"
      provides: "aiQuestionSettings route constant + path + canAccessRoute entry"
    - path: "lib/core/routes/app_router.dart"
      provides: "GoRoute for aiQuestionSettings wired to AiQuestionSettingsScreen"
    - path: "lib/presentation/views/settings/ai_question_settings_screen.dart"
      provides: "Full AiQuestionSettingsScreen widget with API Key tile + Thư viện Tài liệu section"
  key_links:
    - from: "lib/presentation/views/assignment/teacher/teacher_ai_generate_question_screen.dart"
      to: "lib/presentation/views/settings/ai_question_settings_screen.dart"
      via: "context.pushNamed(AppRoute.aiQuestionSettings) at gear icon tap"
    - from: "lib/core/routes/app_router.dart"
      to: "lib/presentation/views/settings/ai_question_settings_screen.dart"
      via: "GoRoute path registration"
---

<objective>
Create AiQuestionSettingsScreen and wire the gear icon route.

Purpose: Implements REQ 9-01 entirely — dedicated AI settings screen decoupled from general settings, with navigation from gear icon on TeacherAiGenerateQuestionScreen.

Output: 4 files changed. New `aiQuestionSettings` route, new screen with API Key tile + document library section (empty state — upload functionality comes in Plan 05). Gear icon at line 904 updated to use new route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-settings-refactor-document-import/09-CONTEXT.md
@lib/core/routes/route_constants.dart
@lib/core/routes/app_router.dart
@lib/presentation/views/settings/settings_screen.dart
@lib/presentation/views/settings/api_key_setup_screen.dart
@lib/core/constants/design_tokens.dart

<interfaces>
<!-- From route_constants.dart — existing route patterns to follow -->
static const String apiKeySetup = 'api-key-setup';
static const String apiKeySetupPath = '/settings/api-keys';

static const String settings = 'settings';
static const String settingsPath = '/settings';

<!-- Teacher routes in canAccessRoute() teacherRoutes set — add aiQuestionSettings here -->
teacherRoutes = {
  teacherClassList, teacherClassSearch, ..., apiKeySetup, settings, ...
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aiQuestionSettings route to route_constants.dart and app_router.dart</name>
  <files>
    lib/core/routes/route_constants.dart
    lib/core/routes/app_router.dart
  </files>
  <read_first>
    - lib/core/routes/route_constants.dart — entire file (already in context), find insertion points: (1) after `apiKeySetup` constants ~line 316, (2) in teacherRoutes set in canAccessRoute() ~line 432
    - lib/core/routes/app_router.dart — find where ApiKeySetupScreen route is defined, add new GoRoute nearby; find import section
    - lib/presentation/views/settings/settings_screen.dart — check import path pattern for settings screens
  </read_first>
  <behavior>
    - Test 1: `AppRoute.aiQuestionSettings == 'ai-question-settings'`
    - Test 2: `AppRoute.aiQuestionSettingsPath == '/settings/ai-questions'`
    - Test 3: `AppRoute.canAccessRoute('teacher', 'ai-question-settings') == true`
    - Test 4: `AppRoute.canAccessRoute('student', 'ai-question-settings') == false`
  </behavior>
  <action>
**In `route_constants.dart`:**

After the `apiKeySetup` constants (around line 316), add:
```dart
/// Teacher - AI Question Settings screen (dedicated AI config + document library)
static const String aiQuestionSettings = 'ai-question-settings';
static const String aiQuestionSettingsPath = '/settings/ai-questions';
```

In the `teacherRoutes` set inside `canAccessRoute()` (around line 418-440), add `aiQuestionSettings`:
```dart
final teacherRoutes = {
  // ... existing entries ...
  apiKeySetup,
  aiQuestionSettings,  // ADD THIS
};
```

**In `app_router.dart`:**

Add import at top with other settings imports:
```dart
import 'package:ai_lms_prd/presentation/views/settings/ai_question_settings_screen.dart';
```

Add GoRoute for aiQuestionSettings (place near apiKeySetup route):
```dart
GoRoute(
  name: AppRoute.aiQuestionSettings,
  path: AppRoute.aiQuestionSettingsPath,
  builder: (context, state) => const AiQuestionSettingsScreen(),
),
```
  </action>
  <verify>
    <automated>cd /d/code/Flutter_Android/Flutter_Android/AI_LMS_PRD && flutter analyze lib/core/routes/ --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `AppRoute.aiQuestionSettings == 'ai-question-settings'`
    - `AppRoute.aiQuestionSettingsPath == '/settings/ai-questions'`
    - `aiQuestionSettings` in teacherRoutes set in `canAccessRoute()`
    - GoRoute registered in app_router.dart
    - `flutter analyze lib/core/routes/` exits 0
  </acceptance_criteria>
  <done>Route constants and router wired. `context.pushNamed(AppRoute.aiQuestionSettings)` will navigate to AiQuestionSettingsScreen.</done>
</task>

<task type="auto">
  <name>Task 2: Create AiQuestionSettingsScreen + fix gear icon route</name>
  <files>
    lib/presentation/views/settings/ai_question_settings_screen.dart
    lib/presentation/views/assignment/teacher/teacher_ai_generate_question_screen.dart
  </files>
  <read_first>
    - lib/presentation/views/settings/settings_screen.dart — visual structure and design token usage to match
    - lib/presentation/views/settings/api_key_setup_screen.dart — just the class name and import path
    - lib/core/constants/design_tokens.dart — DesignColors, DesignSpacing, DesignTypography, DesignRadius
    - lib/presentation/views/assignment/teacher/teacher_ai_generate_question_screen.dart lines 895-920 — gear icon onTap (line 904), context.push(AppRoute.settingsPath) to replace
  </read_first>
  <action>
**Create `lib/presentation/views/settings/ai_question_settings_screen.dart`:**

Structure:
- `Scaffold` with `AppBar(title: Text('Cài đặt AI'))`, leading back arrow
- **Section 1: API Key Setup** — `ListTile` with leading `Icon(Icons.vpn_key_outlined)`, title 'Cài đặt API Key', trailing `Icon(Icons.chevron_right)`, `onTap: () => context.pushNamed(AppRoute.apiKeySetup)`
- **Section 2: Thư viện Tài liệu** — section header + empty state widget (Plan 05 will replace with real list)
  - Empty state: `Icon(Icons.folder_open_outlined)` + Text('Chưa có tài liệu nào') + Text('Thêm tài liệu để AI phân tích khi tạo câu hỏi')
  - [+] FAB or OutlinedButton('Thêm tài liệu') — disabled/grayed with `Tooltip('Tính năng sẽ được kích hoạt sau khi cài đặt')` for now (Plan 05 activates it)
- **Section 3: Công cụ** — `ListTile` for "Xuất file mẫu Excel" (D-16-ext):
  - Leading `Icon(Icons.download_outlined)`
  - Title 'Xuất file mẫu Excel'
  - Subtitle 'Tải template chuẩn để Fast Track ($0, ~0.1s)'
  - `onTap: () => _downloadExcelTemplate(context)` — opens URL to static file in Supabase Storage
  - URL constant: `'https://<YOUR_SUPABASE_PROJECT>.supabase.co/storage/v1/object/public/teacher-documents/templates/question_template.xlsx'`
  - Use `url_launcher` if available, else `Clipboard.setData` + SnackBar fallback

Use ALL design tokens:
- Colors: `DesignColors.*`
- Spacing: `DesignSpacing.*`
- Typography: `DesignTypography.*`
- `const` constructors throughout
- `build()` max 50 lines — extract `_buildApiKeySection()`, `_buildDocumentLibrarySection()`, `_buildToolsSection()`

**Fix gear icon in `teacher_ai_generate_question_screen.dart`:**

Find line ~904:
```dart
// BEFORE:
onTap: () => context.push(AppRoute.settingsPath),
// or: onTap: () => context.pushNamed(AppRoute.settings),
```

Replace with:
```dart
// AFTER (per D-02):
onTap: () => context.pushNamed(AppRoute.aiQuestionSettings),
```

Add import at top of teacher_ai_generate_question_screen.dart if not already present:
```dart
import 'package:ai_lms_prd/core/routes/route_constants.dart';
```
  </action>
  <verify>
    <automated>cd /d/code/Flutter_Android/Flutter_Android/AI_LMS_PRD && flutter test test/widget/ai_settings_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ai_question_settings_screen.dart` file exists and has `AiQuestionSettingsScreen` class
    - Screen has API Key Setup ListTile that navigates to `AppRoute.apiKeySetup`
    - Screen has Thư viện Tài liệu section with empty state
    - Screen has Xuất file mẫu Excel ListTile (D-16-ext)
    - Gear icon in teacher_ai_generate_question_screen.dart at ~line 904 now calls `context.pushNamed(AppRoute.aiQuestionSettings)` (NOT settingsPath)
    - `flutter analyze lib/presentation/views/settings/ lib/presentation/views/assignment/teacher/teacher_ai_generate_question_screen.dart --no-pub` exits 0
  </acceptance_criteria>
  <done>AiQuestionSettingsScreen created and gear icon route updated. REQ 9-01 fully implemented.</done>
</task>

</tasks>

<verification>
```bash
flutter analyze lib/core/routes/ lib/presentation/views/settings/ --no-pub
flutter test test/widget/ai_settings_test.dart --no-pub
```
Manual: Run app → Teacher AI Generate Question screen → tap gear icon → lands on AiQuestionSettingsScreen (not general settings).
</verification>

<success_criteria>
- `AppRoute.aiQuestionSettings` route constant defined
- GoRoute wired in app_router.dart
- AiQuestionSettingsScreen created with API Key tile, empty document library, Excel template download
- Gear icon in TeacherAiGenerateQuestionScreen routes to new screen
- `flutter analyze` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-settings-refactor-document-import/09-03-SUMMARY.md`
</output>
