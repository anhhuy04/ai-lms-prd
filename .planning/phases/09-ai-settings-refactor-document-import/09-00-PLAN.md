---
phase: 09-ai-settings-refactor-document-import
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/teacher_file_datasource_test.dart
  - test/unit/question_dto_test.dart
  - test/widget/ai_settings_test.dart
  - test/widget/staging_area_test.dart
  - supabase/functions/process-document-queue/index.test.ts
autonomous: true
requirements:
  - "9-01"
  - "9-02"
  - "9-03"

must_haves:
  truths:
    - "All 5 Wave 0 test stub files exist on disk"
    - "`flutter analyze` passes with no errors after stubs created"
    - "`flutter test test/unit/ test/widget/ --no-pub` runs (some tests skip/pass, none hard-crash)"
    - "TypeScript test stub compiles (tsc --noEmit)"
  artifacts:
    - path: "test/unit/teacher_file_datasource_test.dart"
      provides: "Stub test cases for TeacherFileDataSource upload + DB insert methods"
    - path: "test/unit/question_dto_test.dart"
      provides: "Stub test cases for QuestionDTO unified schema (REQ 9-03)"
    - path: "test/widget/ai_settings_test.dart"
      provides: "Stub test cases for AiQuestionSettingsScreen navigation (REQ 9-01)"
    - path: "test/widget/staging_area_test.dart"
      provides: "Stub test cases for StagingAreaWidget display (REQ 9-03)"
    - path: "supabase/functions/process-document-queue/index.test.ts"
      provides: "Stub test cases for heuristic router, stateful checkpointing, content hashing"
  key_links:
    - from: "test/unit/teacher_file_datasource_test.dart"
      to: "lib/data/datasources/teacher_file_datasource.dart"
      via: "import — stub references future file"
    - from: "test/widget/ai_settings_test.dart"
      to: "lib/presentation/views/settings/ai_question_settings_screen.dart"
      via: "import — stub references future screen"
---

<objective>
Create Wave 0 test stubs for all 5 test files listed in VALIDATION.md.

Purpose: Establish the Nyquist sampling contract before implementation begins. All subsequent plans reference these files in their `<verify>` blocks. Stubs must compile cleanly but can skip/todo all assertions — they exist so `flutter test` and `tsc` can run incrementally during implementation.

Output: 5 stub files with correct imports, test descriptions, and `skip:` or `markTestSkip()` markers. Zero production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ai-settings-refactor-document-import/09-CONTEXT.md
@.planning/phases/09-ai-settings-refactor-document-import/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter unit test stubs</name>
  <files>
    test/unit/teacher_file_datasource_test.dart
    test/unit/question_dto_test.dart
  </files>
  <read_first>
    - .planning/phases/09-ai-settings-refactor-document-import/09-VALIDATION.md — Wave 0 Requirements section for exact test names
    - .planning/phases/09-ai-settings-refactor-document-import/09-CONTEXT.md — D-05 (upload pipeline), D-24 (QuestionDTO schema)
    - pubspec.yaml — confirm `mocktail` is in dev_dependencies
  </read_first>
  <action>
Create two unit test stub files. Each file must:
- Have correct package imports (comment out class-specific imports if classes don't exist yet — use `// ignore: uri_does_not_exist`)
- Use `group()` to mirror the class under test
- Use `test('...', skip: 'W0 stub — implement in Plan 04')` for all test cases
- Pass `flutter analyze` (no lint errors)

**test/unit/teacher_file_datasource_test.dart**
```dart
// ignore_for_file: depend_on_referenced_packages
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';

// W0 stub — TeacherFileDataSource will be created in Plan 04
// import 'package:ai_lms_prd/data/datasources/teacher_file_datasource.dart';

void main() {
  group('TeacherFileDataSource', () {
    group('uploadFile', () {
      test(
        'uploads bytes to Supabase Storage and returns public URL',
        skip: 'W0 stub — implement when TeacherFileDataSource exists (Plan 04)',
        () async {},
      );
      test(
        'throws FileUploadException on storage failure',
        skip: 'W0 stub — implement when TeacherFileDataSource exists (Plan 04)',
        () async {},
      );
    });

    group('saveFileMetadata', () {
      test(
        'inserts row into files table and returns file id',
        skip: 'W0 stub — implement when TeacherFileDataSource exists (Plan 04)',
        () async {},
      );
      test(
        'inserts file_links row with target_type=teacher',
        skip: 'W0 stub — implement when TeacherFileDataSource exists (Plan 04)',
        () async {},
      );
    });

    group('enqueueForProcessing', () {
      test(
        'inserts ai_queue row with action=vectorize_document',
        skip: 'W0 stub — implement when TeacherFileDataSource exists (Plan 04)',
        () async {},
      );
    });

    group('getTeacherFiles', () {
      test(
        'returns list of TeacherFileModel for given teacher_id',
        skip: 'W0 stub — implement when TeacherFileDataSource exists (Plan 04)',
        () async {},
      );
    });
  });
}
```

**test/unit/question_dto_test.dart**
```dart
// ignore_for_file: depend_on_referenced_packages
import 'package:flutter_test/flutter_test.dart';

// W0 stub — QuestionDTO will be part of AI generate question screen refactor
// import 'package:ai_lms_prd/data/models/question_dto.dart';

void main() {
  group('QuestionDTO', () {
    group('fromJson', () {
      test(
        'parses multiple_choice question with 4 choices',
        skip: 'W0 stub — implement in Plan 07',
        () {},
      );
      test(
        'parses true_false question',
        skip: 'W0 stub — implement in Plan 07',
        () {},
      );
      test(
        'parses short_answer question',
        skip: 'W0 stub — implement in Plan 07',
        () {},
      );
      test(
        'returns null for malformed JSON',
        skip: 'W0 stub — implement in Plan 07',
        () {},
      );
    });

    group('toAssignmentQuestionInsert', () {
      test(
        'maps QuestionDTO to custom_content jsonb format',
        skip: 'W0 stub — implement in Plan 07',
        () {},
      );
    });

    group('Pipeline agnosticism', () {
      test(
        'extraction pipeline and generation pipeline produce identical DTO schema',
        skip: 'W0 stub — implement in Plan 07',
        () {},
      );
    });
  });
}
```
  </action>
  <verify>
    <automated>cd /d/code/Flutter_Android/Flutter_Android/AI_LMS_PRD && flutter test test/unit/teacher_file_datasource_test.dart test/unit/question_dto_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at correct paths
    - `flutter test` reports all tests as "skipped", 0 failures, 0 errors
    - `flutter analyze test/unit/teacher_file_datasource_test.dart test/unit/question_dto_test.dart` has no errors
  </acceptance_criteria>
  <done>Both unit stub files exist, compile cleanly, all tests skip.</done>
</task>

<task type="auto">
  <name>Task 2: Create Flutter widget test stubs</name>
  <files>
    test/widget/ai_settings_test.dart
    test/widget/staging_area_test.dart
  </files>
  <read_first>
    - .planning/phases/09-ai-settings-refactor-document-import/09-CONTEXT.md — D-01/D-02/D-03 (AiQuestionSettingsScreen), D-26/D-27 (Staging Area)
    - .planning/phases/09-ai-settings-refactor-document-import/09-VALIDATION.md — Per-Task Verification Map for exact test descriptions
  </read_first>
  <action>
Create two widget test stub files.

**test/widget/ai_settings_test.dart**
```dart
// ignore_for_file: depend_on_referenced_packages
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

// W0 stub — AiQuestionSettingsScreen will be created in Plan 03
// import 'package:ai_lms_prd/presentation/views/settings/ai_question_settings_screen.dart';

void main() {
  group('AiQuestionSettingsScreen', () {
    group('Navigation', () {
      testWidgets(
        'displays API Key Setup tile',
        skip: 'W0 stub — implement when AiQuestionSettingsScreen exists (Plan 03)',
        (tester) async {},
      );
      testWidgets(
        'tapping API Key Setup tile navigates to ApiKeySetupScreen',
        skip: 'W0 stub — implement when AiQuestionSettingsScreen exists (Plan 03)',
        (tester) async {},
      );
      testWidgets(
        'displays Thư viện Tài liệu section',
        skip: 'W0 stub — implement when AiQuestionSettingsScreen exists (Plan 03)',
        (tester) async {},
      );
      testWidgets(
        'displays Export Excel Template button',
        skip: 'W0 stub — implement when AiQuestionSettingsScreen exists (Plan 03)',
        (tester) async {},
      );
    });

    group('Gear icon route', () {
      testWidgets(
        'gear icon in TeacherAiGenerateQuestionScreen navigates to ai-question-settings route',
        skip: 'W0 stub — implement after route change in Plan 03',
        (tester) async {},
      );
    });

    group('File library', () {
      testWidgets(
        'shows shimmer loading while fetching teacher files',
        skip: 'W0 stub — implement in Plan 05',
        (tester) async {},
      );
      testWidgets(
        'shows empty state when no files uploaded',
        skip: 'W0 stub — implement in Plan 05',
        (tester) async {},
      );
    });
  });
}
```

**test/widget/staging_area_test.dart**
```dart
// ignore_for_file: depend_on_referenced_packages
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

// W0 stub — StagingAreaWidget will be created in Plan 07
// import 'package:ai_lms_prd/presentation/views/assignment/teacher/widgets/staging_area_widget.dart';

void main() {
  group('StagingAreaWidget', () {
    group('Display', () {
      testWidgets(
        'renders list of QuestionDTO items',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
      testWidgets(
        'shows question text and answer choices for each item',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
      testWidgets(
        'shows empty state when questions list is empty',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
    });

    group('Actions', () {
      testWidgets(
        'Lưu vào Ngân hàng button calls saveToBank callback',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
      testWidgets(
        'Lưu và Thêm vào Đề thi button calls saveAndAdd callback',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
      testWidgets(
        'loading indicator shown while saving',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
    });

    group('DraggableScrollableSheet', () {
      testWidgets(
        'sheet starts at half screen height',
        skip: 'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
        (tester) async {},
      );
    });
  });
}
```
  </action>
  <verify>
    <automated>cd /d/code/Flutter_Android/Flutter_Android/AI_LMS_PRD && flutter test test/widget/ai_settings_test.dart test/widget/staging_area_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at correct paths
    - `flutter test` reports all tests as "skipped", 0 failures, 0 errors
    - `flutter analyze` has no errors on these files
  </acceptance_criteria>
  <done>Both widget stub files exist, compile cleanly, all tests skip.</done>
</task>

<task type="auto">
  <name>Task 3: Create Edge Function TypeScript test stub</name>
  <files>
    supabase/functions/process-document-queue/index.test.ts
  </files>
  <read_first>
    - .planning/phases/09-ai-settings-refactor-document-import/09-CONTEXT.md — D-13 (Heuristic Router), D-29 (Stateful Checkpointing), D-30 (Content Hashing)
    - supabase/functions/process-ai-queue/index.ts — existing Edge Function pattern to match test structure
  </read_first>
  <action>
Create the TypeScript test stub for the process-document-queue Edge Function.
This file lives alongside the future Edge Function (not yet created).
Create the `process-document-queue/` directory first if needed.

**supabase/functions/process-document-queue/index.test.ts**
```typescript
/**
 * W0 Test Stubs — process-document-queue Edge Function
 *
 * Tests for:
 *   - Heuristic Router (D-13): Excel Fast Track vs LLM Fallback
 *   - Stateful Checkpointing (D-29): Resume from processed_chunks
 *   - Content Hashing / Differential Update (D-30): SHA-256, Set Difference
 *
 * Run: deno test --allow-net --allow-env index.test.ts
 * (Will be expanded in Plan 06 when index.ts is implemented)
 */

import { assertEquals, assertExists } from 'https://deno.land/std@0.208.0/assert/mod.ts';

// ---------------------------------------------------------------------------
// Heuristic Router (D-13)
// ---------------------------------------------------------------------------

Deno.test({
  name: 'heuristicRouter: recognises standard Excel headers → Fast Track',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {
    // const rows = [{ 'Câu hỏi': '...', 'Đáp án A': '...', 'Đáp án đúng': 'A' }];
    // const result = routeExcel(rows);
    // assertEquals(result.path, 'fast_track');
  },
});

Deno.test({
  name: 'heuristicRouter: non-standard headers → LLM Fallback',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {
    // const rows = [{ 'Nội dung': '...', 'Trả lời': '...' }];
    // const result = routeExcel(rows);
    // assertEquals(result.path, 'llm_fallback');
  },
});

Deno.test({
  name: 'heuristicRouter: .docx content always routes to LLM Fallback',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

// ---------------------------------------------------------------------------
// Stateful Checkpointing (D-29)
// ---------------------------------------------------------------------------

Deno.test({
  name: 'checkpointing: initialises result with total_chunks and processed_chunks=0',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

Deno.test({
  name: 'checkpointing: increments processed_chunks after each batch',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

Deno.test({
  name: 'checkpointing: on retry, skips already-processed chunks',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

Deno.test({
  name: 'checkpointing: does not produce duplicate vectors on retry',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

// ---------------------------------------------------------------------------
// Content Hashing / Differential Update (D-30)
// ---------------------------------------------------------------------------

Deno.test({
  name: 'contentHash: SHA-256 of identical text produces identical hash',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

Deno.test({
  name: 'contentHash: new chunks (hash in new but not old) are embedded and inserted',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

Deno.test({
  name: 'contentHash: stale chunks (hash in old but not new) are deleted',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

Deno.test({
  name: 'contentHash: unchanged chunks (hash in both) are skipped entirely',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {},
});

// ---------------------------------------------------------------------------
// Gemini Embedding API shape (D-18)
// ---------------------------------------------------------------------------

Deno.test({
  name: 'geminiEmbedding: returns 768-element float array',
  ignore: true, // W0 stub — implement in Plan 06
  fn() {
    // const result = await callGeminiEmbedding('test text', 'fake-api-key');
    // assertEquals(result.length, 768);
  },
});
```
  </action>
  <verify>
    <automated>ls /d/code/Flutter_Android/Flutter_Android/AI_LMS_PRD/supabase/functions/process-document-queue/index.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `supabase/functions/process-document-queue/index.test.ts`
    - File has test stubs for all 3 critical D-decisions: D-13, D-29, D-30
    - All tests use `ignore: true` (W0 stub marker)
    - TypeScript syntax is valid (no obvious errors)
  </acceptance_criteria>
  <done>TypeScript test stub exists with correct structure covering heuristic router, checkpointing, and content hashing.</done>
</task>

</tasks>

<verification>
After all tasks complete:
```bash
flutter analyze test/ --no-pub
flutter test test/unit/teacher_file_datasource_test.dart test/unit/question_dto_test.dart test/widget/ai_settings_test.dart test/widget/staging_area_test.dart --no-pub
```
All tests should be "skipped" (0 failures, 0 errors).
</verification>

<success_criteria>
- All 5 Wave 0 test stub files exist on disk
- `flutter test test/unit/ test/widget/ --no-pub` exits 0
- `flutter analyze test/` exits with no errors
- VALIDATION.md `nyquist_compliant` can be updated to `true` after Wave 1 execution begins
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-settings-refactor-document-import/09-00-SUMMARY.md`
</output>
