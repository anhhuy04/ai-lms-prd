---
phase: settings-ui-refactor
status: paused
last_updated: 2026-04-04T23:00:00+07:00
---

<current_state>
Ollama integration vào AI question generation đã hoàn thành. 3 files thay đổi chưa commit.
</current_state>

<completed_work>
- Fix: _ollamaAvailableModels luôn rỗng → populate sau mỗi test/connect OK
- Fix: Auto-fetch models khi mở screen nếu URL đã lưu
- Fix: Row preset chips overflow → Wrap
- Fix: callActiveAi bỏ qua Ollama, default về Gemini → thêm providerOllama routing
- Thêm: callOllamaChat (POST /api/generate, stream:false, timeout 120s)
- Thêm: URL preset chips: "Kiểm tra trên PC"(127.0.0.1), "Kiểm tra từ Emulator"(dialog), "Dùng trên PC Mạng"(localhost)
- Thêm: Manual model TextField + model chips
- Thêm: BottomSheet chọn model (auto-fetch nếu chưa có, list tile + manual input)
- Tối ưu Dio: 3 static instances, _parseHttpError, _parseDioNetworkError, _parseOllamaModels helper
- Thêm: CancelToken support cho testOllamaConnection
- Thêm: providerOllama constant trong AiService
- Thêm: Dialog "Kiểm tra từ Emulator" có hướng dẫn LDPlayer/BlueStacks step-by-step
</completed_work>

<remaining_work>
- Test end-to-end: kết nối Ollama từ LDPlayer → chọn model → tạo câu hỏi AI
- (Optional) Thêm analytics provider routing cho Ollama nếu cần
</remaining_work>

<decisions_made>
- Ollama dùng /api/generate với stream:false (đơn giản hơn /api/chat)
- Static Dio instances thay vì tạo mới mỗi request
- BottomSheet auto-fetch nếu URL có nhưng models list rỗng
- LDPlayer cần host LAN IP + OLLAMA_HOST=0.0.0.0:11434 + firewall port 11434
</decisions_made>

<blockers>
- Không có blocker code. User cần cấu hình Ollama trên host machine.
</blockers>

<context>
User dùng LDPlayer. Ollama mặc định chỉ nghe 127.0.0.1 nên LDPlayer không kết nối được.
Cần: ipconfig → lấy LAN IP → OLLAMA_HOST=0.0.0.0:11434 → mở firewall port 11434 → nhập IP vào app.
</context>

<next_action>
1. Hot reload app
2. Settings → API Key → chọn Ollama → bấm "Kiểm tra từ Emulator" → nhập LAN IP
3. Bấm "Kiểm tra & chọn model" → chọn model
4. Bấm "Cập nhật" để lưu
5. Vào tạo câu hỏi AI → test generation
</next_action>
