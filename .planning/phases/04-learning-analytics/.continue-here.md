---
phase: 04-learning-analytics
task: uat_retest
total_tasks: null
status: paused
last_updated: 2026-03-21T14:20:32.129Z
---

<current_state>
Tất cả 8 issues từ UAT tests 7-12 đã được fix. APK đã build + install trên emulator.
Đang chờ user test Test 3 (Metric Cards + Filter Bottom Sheet).

APK location: build/app/outputs/flutter-apk/app-debug.apk
Device: SM S908EZGHXXV (emulator-5556)
Install command: "C:\Users\AnhHuy\AppData\Local\Android\Sdk\platform-tools\adb.exe" install -r build/app/outputs/flutter-apk/app-debug.apk
</current_state>

<completed_work>

### FIX 1: BLOCKER - Data classId filter (Test 11)
- Root cause: submissions table KHÔNG có class_id column. Supabase silently ignores non-existent column.
- Fix: Fetch distributionIds from assignment_distributions WHERE class_id=classId, then filter submissions by .inFilter('assignment_distribution_id', distributionIds)
- Files: analytics_datasource.dart (all methods: getClassAnalytics, getBasicMetrics, getSkillMastery, getGradeTrends)

### FIX 2: Heatmap UI (Test 7)
- Padding: thêm padding vào chart content
- Truncate: maxLines=2 + ellipsis đã có sẵn
- Colors: 0-4=red, 4-6=amber, 6-8=orange, 8-10=green (đảo index đúng)
- Files: grade_distribution_heatmap.dart

### FIX 3: Class Overview Card (Test 8)
- Score format: dùng toRawString() thay vì toPercent() → "8.0/10"
- Submission rate: thêm metric submissionRate vào ClassOverviewCard
- Position: đã đúng vị trí đầu screen
- Files: class_overview_card.dart, teacher_analytics_screen.dart

### FIX 4: Top/Bottom Performers (Test 9)
- Score format: /10 thay vì %
- Dedup logic: Set<String> topIds loại trừ students trùng ở cả 2 danh sách
- Tap navigation: tạo TeacherStudentAnalyticsScreen mới + route teacherStudentAnalytics
- Files: top_performers_list.dart, bottom_performers_list.dart, teacher_student_analytics_screen.dart, app_router.dart, route_constants.dart, analytics_providers.dart

### FIX 5: Line Chart (Test 2)
- Score format: /10 thay vì % (Y-axis, tooltip)
- Horizontal scroll: đã có sẵn
- Files: line_trend_chart.dart

### FIX 6: Metric Cards (Test 3)
- Score format: /10 thay vì %
- Trend indicator: thêm Icons.trending_flat cho stable, dùng DesignColors.textSecondary cho default
- Files: metric_card.dart, student_analytics_screen.dart

### FIX 7: Time Filter Bottom Sheet (Test 5)
- Redesign: chips → bottom sheet với time range + class filter riêng biệt
- Bottom sheet content: "Tuần này", "Tháng này", "Học kỳ", "Tất cả", "Tùy chỉnh..." + danh sách lớp
- Class filter: dùng studentClassesForAnalyticsProvider
- Filter wired to ALL data (metrics + charts) qua provider
- Files: time_range_selector.dart, student_analytics_screen.dart, analytics_providers.dart

### FIX 8: Shimmer Loading (Test 12)
- ShimmerTeacherAnalyticsLoading: skeleton cho Teacher Analytics (overview card + heatmap + top/bottom)
- ShimmerStudentAnalyticsLoading: skeleton cho Student Analytics (time chips + 2x2 cards + radar + line)
- Files: shimmer_loading.dart, teacher_analytics_screen.dart, student_analytics_screen.dart

### BUILD FIXES:
- analytics_datasource.dart: Fix PostgREST query chain (.gte/.lte phải trước .select())
- labelLarge → titleMedium (DesignTypography không có labelLarge method)
- AnalyticsFilterChip: thêm required hasActiveFilter parameter
- showAnalyticsFilterBottomSheet: đúng signature (ref, initialTimeRange)
- Import fixes: thêm AnalyticsTimeRangeWeek/Month/Semester exports

</completed_work>

<remaining_work>

- Test 3 (đang chờ): Metric Cards /10 + trend indicator + bottom sheet filter
- Test 7-12: Teacher Analytics re-test sau khi fix
- Commit all changes sau khi UAT pass

</remaining_work>

<decisions_made>

- submissions table có assignment_distribution_id, KHÔNG có class_id. Filter phải qua assignment_distributions.
- Score display: dùng ScoreDisplayUtils.toRawString() cho /10 thay vì toPercent()
- Time filter: bottom sheet với time range + class filter tách riêng
- Dedup top/bottom: Set<String> topIds để loại trừ overlap
- Shimmer: widgets riêng cho từng screen thay vì generic

</decisions_made>

<blockers>

- User đang test Test 3 - đang chờ feedback

</blockers>

<context>
Phase 04 Learning Analytics UAT. Tất cả 8 issues (1 blocker + 7 major) đã được fix bởi agents + manual fixes.
APK đã install trên emulator. User đang test Test 3. Cần commit sau khi UAT hoàn thành.
</context>

<next_action>
Đợi user test Test 3 (Metric Cards /10 + trend + bottom sheet). Sau đó test lại Test 7-12.
Sau khi pass: commit all 20+ uncommitted files.
</next_action>
