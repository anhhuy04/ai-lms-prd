---
phase: "05a"
plan: "05-06"
type: gap-closure
wave: 1
depends_on: ["05"]
gap_closure: true
created: 2026-05-31
decisions_locked:
  - "Q1: XÓA 19 dòng generator cũ (teacher_id NULL), để edge sinh lại — KHÔNG backfill (đã xác nhận)"
  - "Q2: EDGE là generator canonical; Dart _handleAnalysis → no-op"
  - "Q3: 5a CÓ thêm cảnh báo rule-based (at_risk_warning) — KHÔNG dùng LLM"
  - "Q4: Tách REC-01 (teacher) / REC-02 (student) NGAY + cột subject_student_id chống rò RLS"
files_modified:
  - supabase/migrations/20260601120000_037_ai_recommendations_idempotency_category.sql  # TẠO MỚI
  - supabase/functions/process-ai-queue/index.ts
  - lib/core/services/teacher_ai_queue_processor.dart
  - lib/data/datasources/recommendation_datasource.dart
  - lib/data/datasources/analytics_datasource.dart
  - lib/presentation/views/recommendation/student/student_recommendations_tab.dart
  - lib/presentation/views/recommendation/teacher/teacher_recommendations_screen.dart
requirements:
  - R1   # generator set teacher_id + class_id; edge canonical; xóa 19 dòng NULL
  - R2   # idempotency (full unique index + upsert) gộp dedup vào delete
  - R3   # tách scope(type) khỏi category + fix _typeStringToEnum đọc sai cột
  - G1   # standalone route — VERIFY no-op (đã đủ)
  - G2   # invalidate(top3) trong if(success) — VERIFY no-op (đã đúng)
  - G3   # RPC class average — đã tồn tại; chỉ fix numeric-as-string parse
  - empty-state
  - "Q3-warnings"   # NEEDS-VERIFY: cảnh báo rule-based at_risk_warning
  - "Q4-split"      # NEEDS-VERIFY: REC-01/REC-02 + subject_student_id
provenance: >
  Tổng hợp từ workflow wf_6678f992-770 (4 đặc vụ thiết kế + 4 cross-review đối kháng + 1 synth).
  Phần PART 1 đã VERIFY thực nghiệm trên DB sống. Phần PART 2 (Q3+Q4) là mở rộng theo
  quyết định user, CHƯA verify — phải chạy lượt verify (Task 6 pre-checks) trước khi code.
---

<objective>
Sửa recommendation engine RULE-BASED của Phase 5 cho "sống" thật: recommendations tự sinh
phải có teacher_id để teacher đọc được (RLS teacher_id=auth.uid()), idempotent (không trùng
"Ôn tập: FL.01"), phân loại đúng (category tách khỏi scope), radar lớp hiển thị số thật,
empty-state không khẳng định sai. Thêm: tách REC-01 (cảnh báo cho teacher) / REC-02 (gợi ý
cho student) + cảnh báo rule-based at_risk. TUYỆT ĐỐI KHÔNG LLM/RAG — đó là 5b/Phase 6.
</objective>

<scope-split>
## PART 1 — ĐÃ VERIFY (sẵn sàng execute)  ← cross-review xác nhận trên DB sống
Tasks 1–5: migration mechanics, upsert, omit dismissed/created_at, category, avg_mastery fix,
empty-state, no-op G1/G2/G3. Mọi "sự thật" đã chạy Supabase MCP + đọc code thật.

## PART 2 — Q3 + Q4 (CẦN VERIFY trước khi code)  ← mở rộng theo quyết định user
Task 6: cột subject_student_id + index unique thứ 2 (teacher rows) + cảnh báo at_risk +
tách dòng REC-01/REC-02. 4 điểm phải verify (xem <needs-verify>).
</scope-split>

<facts-verified>
Bằng chứng đã chạy (Supabase MCP + Read), KHÔNG suy đoán:
- ai_recommendations: KHÔNG có cột category/objective_id, KHÔNG unique index ngoài pkey.
  type CHECK ∈ {individual, small_group, class}. priority CHECK 1..5.
  class_id FK→classes; teacher_id/student_id FK→auth.users.
- Data sống: total=50, null_teacher=19, source='ai_queue_analysis'=19, source='teacher_local_processor'=0,
  seed(teacher_id NOT NULL)=31, student_with_obj=19, seed_with_obj=0.
- 19 dòng NULL = 19 dòng trùng = 19 dòng có objective_id (9 bản student 076de02d obj 3b7b79a4 + 5×2 bản
  student b3e27ac4). XÓA 19 GỘP LUÔN dedup → còn 31. (con số "37" của review B là nhánh keep, ta chọn delete.)
- TEST 42P10 (chạy thật): PARTIAL unique index + bare ON CONFLICT (dạng PostgREST .upsert) → FAIL 42P10.
  FULL unique index + bare ON CONFLICT → SUCCESS; (NULL,NULL) chèn 2 lần → 2 dòng (NULLS DISTINCT) →
  seed/teacher rows tự thoát ràng buộc.
- PostgREST serialize numeric TABLE-column → CHUỖI: get_class_average_skill_mastery() trả
  avg_mastery="0.00000000000000000000" → (r['avg_mastery'] as num?) = null → 0.0 mọi objective.
  Radar lớp ĐANG TRẮNG 0 (bug sống).
- distribution_type live ∈ {class(42), group(1)}; class_id NOT NULL ở cả 43 dòng (cột vẫn nullable → giữ xử lý NULL).
- RLS: policy ALL cũ "Teachers can manage" (USING auth.uid()=teacher_id, WITH CHECK NULL) + 4 policy mới
  (teachers_read/update, students_read/update). INSERT teacher_id=NULL bị chặn → null_local=0 dù Dart chạy lâu nay
  → EDGE (service-role bypass RLS) là generator DUY NHẤT đang sống.
- Edge handleAnalysis: session select "student_id, assignment_distribution_id"; insert source="ai_queue_analysis",
  dismissed:false, created_at:now. handleFeedback (cùng file) đã có mẫu resolve teacher_id qua
  assignment_distributions → assignments!inner(teacher_id).
- Dart _handleAnalysis (teacher_ai_queue_processor.dart L127-181): session select 'student_id', insert
  source 'teacher_local_processor', dismissed:false.
- recommendation_datasource.dart: getTeacher L23 .eq teacher_id, getStudent L56 .eq student_id, cả 2 .eq('dismissed',false).
  _mapRowToRecommendation L154-174 đọc type=row['type'] (L157) → _typeStringToEnum(type) L166.
  _typeStringToEnum L176-199: 9 case + default studyTip. row['type']=scope → KHÔNG match → 100% rơi studyTip.
- recommendation.dart _typeToString L219-240: 9 chuỗi gồm 'at_risk_warning' (KHÔNG phải 'at_risk') + 'improvement_opportunity'.
- recommendation_providers.dart L235-242: invalidate(top3) L241 NẰM TRONG if(success){ (L235..L242). → G2 FALSE POSITIVE.
- app_router.dart: shell route '/student/recommendations' (L234-241) + standalone '/student/recommendations/view'
  name '..._standalone' (L499-501); route_constants.dart:429 trong studentRoutes RBAC. → G1 FALSE POSITIVE (đã đủ).
- student_dashboard_providers.dart:62 studentRecentScores: status=='graded' && total_score!=null, take(5). → tín hiệu "đã có bài chấm".
- Migration canonical dir = supabase/migrations (mới nhất ..._036). db/migrations là legacy KHÔNG được CLI apply.
</facts-verified>

<resolved-conflicts>
Cross-review đã giải quyết (chọn + lý do), KHÔNG để mở:
1. KHÓA IDEMPOTENCY: CHỌN B (FULL index + cột objective_id thật). Bỏ partial/generated của A (42P10 + jsonb mong manh).
2. CHIẾN LƯỢC DÒNG: CHỌN XÓA 19 (Q1). 1 DELETE gộp dedup; teacher_id không backfill được; tổng còn 31.
3. PAYLOAD: teacher_id/class_id LUÔN gửi; dismissed + created_at OMIT (chống resurrect rec đã ẩn + dời mốc tạo).
4. CỘT category: 1 migration duy nhất sở hữu, NOT NULL DEFAULT 'study_tip' + CHECK 9 giá trị khớp _typeToString
   ('at_risk_warning' KHÔNG phải 'at_risk').
5. _typeStringToEnum: đọc row['category'] (KHÔNG dùng row['category'] ?? row['type']); tránh biến 'type' unused.
6. avg_mastery: FIX BẮT BUỘC (double.tryParse), không verify-only.
7. G1/G2: VERIFY no-op. G3 RPC đã tồn tại; chỉ fix parse.
8. MIGRATION: 1 file supabase/migrations/...037 (delete → add columns → FK → index). Edge redeploy 1 lần SAU.
9. EDGE canonical; Dart _handleAnalysis → no-op (null_local=0; sửa Dart vượt RLS sẽ tạo nguồn-trùng).
OUT-OF-SCOPE: dọn policy ALL cũ "Teachers can manage" (hygiene sau).
</resolved-conflicts>

<tasks>

<!-- ===================== PART 1 — ĐÃ VERIFY ===================== -->

<task id="1" name="Migration 037 — delete 19 + objective_id + category + FK + full unique index">
files:
  - supabase/migrations/20260601120000_037_ai_recommendations_idempotency_category.sql (TẠO MỚI)

action: 1 migration duy nhất, apply qua Supabase MCP apply_migration. THỨ TỰ: DELETE trước (gộp dedup)
→ ADD COLUMN → FK → CREATE UNIQUE INDEX cuối (index trước delete sẽ fail trùng).
LƯU Ý: nếu làm Q4 (Task 6) thì migration này sẽ THÊM subject_student_id + index thứ 2 — gộp chung 1 file 037
(xem Task 6 để tránh tạo 2 migration đụng cột). Ở PART 1 thuần, đoạn SQL dưới là tối thiểu.

sql: |
  BEGIN;
  -- 1) DỌN 19 dòng generator cũ (teacher_id NULL) — gộp luôn dedup. Edge sinh lại đúng on-submit. Còn 31.
  DELETE FROM public.ai_recommendations
  WHERE teacher_id IS NULL AND resources->>'source' = 'ai_queue_analysis';

  -- 2) Cột thật objective_id (nullable: seed/teacher rows không có objective).
  ALTER TABLE public.ai_recommendations ADD COLUMN IF NOT EXISTS objective_id uuid;

  -- 3) Cột category (tách khỏi type=scope). NOT NULL DEFAULT 'study_tip' → 31 dòng cũ tự nhận default.
  ALTER TABLE public.ai_recommendations ADD COLUMN IF NOT EXISTS category text NOT NULL DEFAULT 'study_tip';
  ALTER TABLE public.ai_recommendations DROP CONSTRAINT IF EXISTS ai_recommendations_category_check;
  ALTER TABLE public.ai_recommendations ADD CONSTRAINT ai_recommendations_category_check
    CHECK (category = ANY (ARRAY['study_tip','intervention','peer_comparison','assignment_suggestion',
      'skill_gap','engagement_alert','at_risk_warning','improvement_opportunity','late_submission_alert']::text[]));

  -- 4) FK objective_id → learning_objectives ON DELETE SET NULL.
  ALTER TABLE public.ai_recommendations ADD CONSTRAINT ai_recommendations_objective_id_fkey
    FOREIGN KEY (objective_id) REFERENCES public.learning_objectives(id) ON DELETE SET NULL;

  -- 5) FULL unique index (NULLS DISTINCT) — KHÔNG partial (42P10 verified). Seed rows (objective_id NULL) tự thoát.
  CREATE UNIQUE INDEX IF NOT EXISTS idx_ai_recs_student_objective_unique
    ON public.ai_recommendations (student_id, objective_id);
  COMMIT;

verify:
  - SELECT count(*) FROM public.ai_recommendations;                      -- KỲ VỌNG 31
  - SELECT count(*) FROM public.ai_recommendations WHERE teacher_id IS NULL;  -- KỲ VỌNG 0 (seed đều có teacher_id)
  - SELECT conname FROM pg_constraint WHERE conrelid='public.ai_recommendations'::regclass
      AND conname IN ('ai_recommendations_category_check','ai_recommendations_objective_id_fkey'); -- 2 dòng
  - SELECT indexname FROM pg_indexes WHERE tablename='ai_recommendations'
      AND indexname='idx_ai_recs_student_objective_unique';             -- 1 dòng
  - SELECT category, count(*) FROM public.ai_recommendations GROUP BY category;  -- tất cả 'study_tip'

acceptance_criteria:
  - Apply không lỗi (CREATE UNIQUE INDEX không ném duplicate vì DELETE dọn trước).
  - Tổng = 31; tất cả category='study_tip'; index + FK + check tồn tại.
  - GATE: PHẢI xong TRƯỚC Task 2 (deploy edge .upsert). Đảo thứ tự → 42P10 ngay submit đầu.
</task>

<task id="2" name="Edge generator → upsert + resolve teacher_id/class_id + category; omit dismissed/created_at; Dart no-op">
files:
  - supabase/functions/process-ai-queue/index.ts
  - lib/core/services/teacher_ai_queue_processor.dart

action: Sửa edge handleAnalysis (canonical) thành upsert; redeploy. Tắt Dart _handleAnalysis no-op.
LƯU Ý: nếu làm Q4 (Task 6) thì block insert dưới sẽ tách thành 2 upsert (student row + teacher row).
Ở PART 1 thuần là 1 upsert có cả teacher_id (1 dòng cả 2 cùng thấy).

edge — resolve (thêm NGAY SAU session, mẫu handleFeedback):
  const { data: dist } = await supabase.from("assignment_distributions")
    .select("class_id, assignments!inner(teacher_id)").eq("id", session.assignment_distribution_id).single();
  const teacherId = (dist?.assignments as Record<string, unknown>)?.teacher_id as string | null ?? null;
  const classId = (dist?.class_id as string | null) ?? null;

edge — insert→upsert (THÊM teacher_id/class_id/objective_id/category; BỎ dismissed + created_at):
  await supabase.from("ai_recommendations").upsert({
    student_id: session.student_id, teacher_id: teacherId, class_id: classId,
    objective_id: m.objective_id, type: "individual", category: "study_tip",
    priority: Math.min(5, Math.max(1, Math.round((1 - (m.mastery_level as number)) * 5))),
    title: `Ôn tập: ${code}`,
    description: `${desc} — tỷ lệ thành thạo ${masteryPct}%, cần cải thiện.`,
    resources: { objective_id: m.objective_id, mastery_level: m.mastery_level, attempts: m.attempts,
                 correct_count: m.correct, generated_at: now, source: "ai_queue_analysis" },
    // BỎ dismissed (chống resurrect) + created_at (chống dời mốc). Cột default lo phần insert.
  }, { onConflict: "student_id,objective_id", ignoreDuplicates: false });
  // resources build FULL object mỗi lần (không spread) — Array Annihilation.

dart — teacher_ai_queue_processor.dart: nhánh request_type=='analysis' CHỈ gọi _markCompleted(itemId),
  KHÔNG gọi _handleAnalysis. _handleAnalysis đánh dấu @deprecated, giữ lại, không gọi. KHÔNG đụng watcher.

deploy: sửa index.ts → mcp__supabase__deploy_edge_function(function_slug='process-ai-queue'). PHẢI sau Task 1.

verify:
  - get_edge_function('process-ai-queue') chứa '.upsert(' + 'onConflict' + 'teacher_id: teacherId' + 'category';
    KHÔNG còn 'dismissed: false'/'created_at: now' trong block analysis.
  - flutter analyze teacher_ai_queue_processor.dart → no errors (có thể cần // ignore: unused_element cho _handleAnalysis).
acceptance_criteria:
  - Edge upsert onConflict 'student_id,objective_id', payload có teacher_id/class_id/objective_id/category, KHÔNG dismissed/created_at.
  - Dart nhánh analysis chỉ _markCompleted.
  - GATE: deploy SAU Task 1.
</task>

<task id="3" name="Datasource đọc category + fix avg_mastery numeric-as-string">
files:
  - lib/data/datasources/recommendation_datasource.dart
  - lib/data/datasources/analytics_datasource.dart

action:
  (3a) recommendation_datasource.dart L154-174: đổi `final type = row['type']...` + `_typeStringToEnum(type)`
       → `final category = row['category'] as String? ?? 'study_tip';` + `_typeStringToEnum(category)`.
       Bỏ biến 'type' local nếu không dùng (tránh unused_local_variable). Đổi tên tham số hàm String type→category cho rõ.
  (3b) analytics_datasource.dart L865: `(r['avg_mastery'] as num?)?.toDouble() ?? 0.0`
       → `double.tryParse(r['avg_mastery'].toString()) ?? 0.0`  (PostgREST serialize numeric thành chuỗi → bug radar trắng 0 sống).
verify:
  - flutter analyze 2 file → no errors, no unused_local_variable.
  - Grep 'double.tryParse(r' gần avg_mastery → 1 match; grep '_typeStringToEnum(category)' → 1 match.
acceptance_criteria:
  - RecommendationType map theo category. getClassAverageSkillMastery trả mastery THẬT (0.0-1.0).
</task>

<task id="4" name="Flutter empty-state + verify no-op G1/G2">
files:
  - lib/presentation/views/recommendation/student/student_recommendations_tab.dart
  - lib/presentation/views/recommendation/teacher/teacher_recommendations_screen.dart
  - lib/presentation/providers/recommendation_providers.dart  (VERIFY only)
  - lib/core/routes/app_router.dart                            (VERIFY only)

action:
  (4a) STUDENT — tách 3 trạng thái: hasScores = ref.watch(studentRecentScoresProvider).valueOrNull?.isNotEmpty ?? false.
       recs.isEmpty + !hasScores → (a) 'Chưa có gợi ý' (icon auto_awesome_rounded, textTertiary),
         sub 'Gợi ý học tập sẽ xuất hiện sau khi bạn hoàn thành và được chấm một vài bài.'
       recs.isEmpty + hasScores  → (b) 'Bạn đang học rất tốt!' (thumb_up_rounded, success).
       error branch (c) giữ nguyên. Dùng DesignColors/Typography/Spacing/Icons — KHÔNG hardcode.
  (4b) TEACHER — copy trung tính (không có tín hiệu rẻ): icon insights_outlined,
       'Chưa có gợi ý' + 'Gợi ý sẽ xuất hiện sau khi học sinh hoàn thành bài và hệ thống phân tích kết quả.'
       _buildFilterEmptyState + _buildErrorState giữ nguyên.
  (4c) VERIFY G2 no-op: Read recommendation_providers.dart L235-242 — invalidate(top3) trong if(success). KHÔNG sửa.
  (4d) VERIFY G1 no-op: Read app_router.dart L234-241 + L499-501 + route_constants.dart:429. Route đã đủ. KHÔNG thêm.
       Verify runtime: cold-load '/student/recommendations' land trong shell có bottom nav.
note: hasScores residual risk (edge im lặng fail + có bài chấm → vẫn hiện (b)) — chấp nhận 5a; copy (b) trung tính; nâng 5b.
acceptance_criteria:
  - Student 3 trạng thái; teacher copy trung tính (bỏ "Mọi học sinh đều ổn"); (c) lỗi riêng.
  - Home _buildRecommendationsSection GIỮ SizedBox.shrink() — KHÔNG sửa.
  - G1/G2 xác nhận no-op (Read lại line trước khi kết luận).
</task>

<task id="5" name="Dọn dữ liệu (đã gộp Task 1) + báo QA">
files: []
action: |
  Không có DELETE/backfill thứ 2 — đã gộp vào DELETE Task 1.
  BÁO QA: sau migration, 2 HS test (076de02d, b3e27ac4) MẤT rec generator cũ NHƯNG còn seed rows
  (teacher_id NOT NULL) → tab HS KHÔNG trống (review D bác bỏ crossDep "tab HS sẽ trống" của thiết kế A).
  HS đọc qua student_id bất kể teacher_id. Edge sinh lại đúng on-submit.
acceptance_criteria:
  - Không có SQL thứ 2 động dữ liệu. QA biết cleanup KHÔNG làm tab HS trống.
</task>

<!-- ===================== PART 2 — Q3 + Q4 (CẦN VERIFY) ===================== -->

<task id="6" name="[Q4 split + Q3 warnings] subject_student_id + index thứ 2 + teacher REC-01 rows + cảnh báo at_risk">
status: NEEDS-VERIFY (chạy <needs-verify> trước khi code)
files:
  - supabase/migrations/20260601120000_037_ai_recommendations_idempotency_category.sql  (GỘP vào file 037, KHÔNG tạo file mới)
  - supabase/functions/process-ai-queue/index.ts

design:
  ## Q4 — tách REC-01 (teacher) / REC-02 (student) + chống rò RLS
  - Migration 037 THÊM: `ALTER TABLE ai_recommendations ADD COLUMN IF NOT EXISTS subject_student_id uuid
    REFERENCES auth.users(id);`  (= "rec này NÓI VỀ học sinh nào" — chỉ data, KHÔNG nằm trong RLS policy nào →
    student A không đọc được dòng teacher vì student_id NULL ở đó).
  - Index thứ 2 cho teacher rows (idempotency riêng):
    `CREATE UNIQUE INDEX IF NOT EXISTS idx_ai_recs_teacher_subject_obj_unique
       ON ai_recommendations (teacher_id, subject_student_id, objective_id);`  (FULL, NULLS DISTINCT →
    student rows có 3 cột này NULL tự thoát; chỉ teacher rows bị ràng buộc).
  - Edge handleAnalysis sinh 2 LOẠI dòng cho mỗi objective yếu:
    * REC-02 (student): student_id=A, teacher_id=NULL, category='study_tip', title 'Ôn tập: code'.
      upsert onConflict 'student_id,objective_id'.
    * REC-01 (teacher): teacher_id=T, class_id, student_id=NULL, subject_student_id=A,
      category='intervention' (hoặc 'at_risk_warning' theo Q3), title 'Cần hỗ trợ: <tên HS> — <code>'.
      upsert onConflict 'teacher_id,subject_student_id,objective_id'.

  ## Q3 — cảnh báo rule-based (KHÔNG LLM)
  - at_risk_warning: khi mastery RẤT thấp (ngưỡng đề xuất < 0.3 — CẦN VERIFY phân bố thật) → dòng REC-01
    category='at_risk_warning', priority=1.
  - intervention: 0.3 ≤ mastery < 0.6 → dòng REC-01 category='intervention', priority theo mastery.
  - late_submission_alert / engagement_alert: CẦN nguồn dữ liệu khác (deadline/submission timing) mà
    generator hiện KHÔNG query → ĐỀ XUẤT DEFER sang 5b, HOẶC mở rộng query (xem needs-verify #4).

verify (sau khi code):
  - SELECT count(*) FROM ai_recommendations WHERE teacher_id IS NOT NULL AND subject_student_id IS NOT NULL
      AND resources->>'source'='ai_queue_analysis';  -- > 0 sau 1 submit HS yếu.
  - Gọi edge 2 lần cùng session → 0 dòng trùng (teacher_id,subject_student_id,objective_id).
  - RLS: query bằng student A KHÔNG trả dòng teacher (student_id NULL). query bằng teacher T trả dòng intervention.
  - InterventionBadge count (recommendation_providers.dart interventionCountProvider) > 0 cho teacher.
acceptance_criteria:
  - Teacher thấy dòng REC-01 (intervention/at_risk_warning) về HS yếu; student KHÔNG thấy dòng teacher (no leak).
  - at_risk_warning hiển thị badge "Rủi ro" đúng (RecommendationCard._buildTypeBadge map atRiskWarning).
  - Idempotency cả 2 loại dòng (2 index full riêng biệt).
</task>

</tasks>

<needs-verify>
## PHẢI VERIFY TRƯỚC KHI CODE TASK 6 (4 điểm — chưa được workflow kiểm chứng)
1. **NULLS DISTINCT trên index 3 cột** `(teacher_id, subject_student_id, objective_id)`: xác nhận student rows
   (3 cột NULL) + seed rows KHÔNG va index này (logic giống index 2-cột đã verify, nhưng test riêng cho chắc).
2. **Ngưỡng at_risk** < 0.3: chạy `SELECT width_bucket(mastery_level,0,1,10), count(*) FROM student_skill_mastery
   GROUP BY 1` để chọn ngưỡng theo phân bố thật, tránh hoặc quá nhiều/quá ít cảnh báo.
3. **Per-objective vs per-student cho REC-01**: dòng teacher mỗi objective yếu có thể FLOOD teacher
   (1 HS 5 objective yếu → 5 dòng intervention). Quyết: gom 1 dòng/HS (key (teacher_id, subject_student_id),
   objective_id=NULL → nhưng NULL phá idempotency) HAY giữ per-objective? → cần chốt + test idempotency tương ứng.
4. **late_submission/engagement**: generator hiện chỉ query student_skill_mastery. 2 cảnh báo này cần
   deadline/submission timing → quyết DEFER 5b hay mở rộng query edge (chi phí thêm).
5. **subject_student_id RLS**: xác nhận KHÔNG policy nào (kể cả policy ALL cũ "Teachers can manage") cho student
   đọc dòng có student_id NULL qua đường khác.
6. **Tên HS trong title REC-01**: cần join profiles để lấy full_name (mẫu handleFeedback đã join profiles.metadata).
</needs-verify>

<verification>
## PATH A — Seed verify nhanh (SQL, không cần app) — sau Task 1+2
1. Baseline: SELECT count(*) total, count(*) FILTER (WHERE teacher_id IS NOT NULL) FROM ai_recommendations; -- 31/31
2. Trigger edge 1 session HS yếu → SELECT student_id,teacher_id,class_id,objective_id,type,category,title
   FROM ai_recommendations WHERE resources->>'source'='ai_queue_analysis' ORDER BY created_at DESC LIMIT 5;
   -- teacher_id/class_id/objective_id NOT NULL, type='individual', category='study_tip'.
3. Idempotency: gọi edge lần 2 → GROUP BY student_id,objective_id HAVING count(*)>1 → 0 dòng.
4. Dismiss-không-resurrect: UPDATE 1 rec dismissed=true; gọi edge lần 3 → rec đó vẫn dismissed=true.
5. TEACHER THẤY: SELECT count(*) FROM ai_recommendations WHERE teacher_id='<T test>' AND dismissed=false; -- >0 (trước fix=0).
6. avg_mastery: SELECT objective_id,avg_mastery FROM get_class_average_skill_mastery('<class_id>') LIMIT 3; app double.tryParse OK.

## PATH B — E2E thật (marionette/dart MCP)
1. flutter analyze toàn dự án → 0 errors.
2. Login teacher (có lớp + HS yếu <0.6). HS nộp 1 bài → edge sinh rec.
3. Teacher mở TeacherRecommendationsScreen → THẤY rec (trước fix không thấy gì).
4. HS mở /student/recommendations → thấy rec + empty-state đúng (a/b).
5. HS dismiss → home top3 cập nhật; nộp lại → rec đã dismiss KHÔNG sống lại.
6. Teacher analytics lớp → radar mastery THẬT (không toàn 0).
7. [Q3/Q4] Teacher thấy cảnh báo at_risk/intervention về HS; HS KHÔNG thấy dòng teacher.
</verification>

<success_criteria>
PART 1 (verified):
- [ ] Migration 037: total=31, category NOT NULL DEFAULT, objective_id+FK+full unique index tồn tại.
- [ ] Edge .upsert(onConflict 'student_id,objective_id') có teacher_id/class_id/objective_id/category; KHÔNG dismissed/created_at; redeploy OK.
- [ ] Dart _handleAnalysis no-op.
- [ ] Datasource _typeStringToEnum đọc category. avg_mastery double.tryParse → radar số thật.
- [ ] Idempotency: edge 2 lần → 0 trùng. Dismiss không resurrect. Teacher đọc được rec (trước=0).
- [ ] Empty-state 3 trạng thái student / trung tính teacher; home giữ SizedBox.shrink().
- [ ] G1/G2/G3 xác nhận no-op (G3 chỉ fix parse). flutter analyze 0 errors. GATE task1→task2 tôn trọng.
PART 2 (Q3+Q4, sau needs-verify):
- [ ] subject_student_id + index thứ 2; teacher REC-01 rows (intervention/at_risk_warning); student không thấy dòng teacher.
- [ ] Cảnh báo at_risk hiển thị badge "Rủi ro"; idempotency cả 2 loại dòng.
</success_criteria>

<open-questions-execution>
Cần chốt khi BẮT ĐẦU execute (không chặn việc duyệt plan):
1. Deploy: redeploy edge thẳng lên project sống (version 9 → 10) hay test Supabase branch trước (create_branch)?
2. Task 6 (Q3/Q4): chạy lượt verify <needs-verify> bằng 1 workflow nhỏ, hay tôi tự verify inline rồi code?
</open-questions-execution>

<deferred-5b>
5b / Phase 6 (KHÔNG làm 5a):
- Thay rule-based bằng LLM (request_type='score' đang stub; analysis hiện chỉ mastery<0.6).
- Resources nội bộ/RAG (pgvector) — AI chỉ chọn ID từ whitelist, backend filter discard ID lạ (đã chốt Option 1).
- late_submission_alert / engagement_alert (cần deadline/timing data).
- Recommendation cấp lớp (type='class') dùng class_id 5a đã set + RPC get_class_average_skill_mastery.
- Empty-state lens chính xác (tín hiệu 'analysis-queue completed' thật, thay hasScores).
- Hygiene: dọn policy ALL cũ "Teachers can manage" trùng cặp teachers_read/update.
</deferred-5b>
