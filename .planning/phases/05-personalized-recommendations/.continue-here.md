---
phase: 05-personalized-recommendations
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-03-25T16:00:34.658Z
---

<current_state>
Phase 05 đang ở trạng thái gap-closure sau UAT. Đã tạo xong gap plan `05-05-PLAN.md` với `gap_closure: true`, nhưng chưa bắt đầu execute.
</current_state>

<completed_work>

- UAT Phase 05 đã chạy xong (`05-UAT.md`) và commit kết quả: `f7011c4`.
- Đã fix blocker dữ liệu recommendations:
  - table `recommendations` -> `ai_recommendations`
  - mapping cột theo schema thực (`teacher_id/student_id`, `priority`, `dismissed`)
- Đã fix UX teacher recommendations:
  - back navigation crash (`Navigator.pop` -> `context.pop`)
  - stack behavior (`goNamed` -> `pushNamed`)
  - filter chip “Khan cap” hoạt động
  - dismiss card mượt + snackbar “Đã xóa gợi ý”
- Đã seed test data cho `ai_recommendations` bằng file:
  - `db/seed_05_recommendations_test_data.sql`
- Đã tạo gap plan mới:
  - `.planning/phases/05-personalized-recommendations/05-05-PLAN.md`
</completed_work>

<remaining_work>

- Task 1: Execute 05-05 plan (Student Home recommendations entry-point)
- Task 2: Add/wire student recommendations route constants + router route
- Task 3: Ensure dismiss invalidates `top3RecommendationsProvider`
- Task 4: Run analyzer/build_runner and verify no errors
- Task 5: Re-run UAT focused tests:
  - Test 8 (Student pillbox/tab "Hoc tap")
  - Test 7 remains blocked nếu chưa có analytics/submissions data
</remaining_work>

<decisions_made>

- Giữ `Recommendation` entity dạng plain class (không migrate Freezed) để tránh blast radius và giữ linh hoạt `metadata`.
- UX dismiss ưu tiên local-state update để mượt, không reload toàn màn.
- UAT kết luận: vấn đề còn lại chính là thiếu entry-point "Hoc tap" ở Student Home, không phải do recommendation tab widget.
</decisions_made>

<blockers>
- Technical: Subagent worktree creation lỗi lock `.git/config` (`could not lock config file .git/config: File exists`).
  - Workaround: chạy planner/executor không isolation hoặc tiếp tục inline.
- Data dependency: PeerComparisonBadge cần dữ liệu submissions/analytics (RPC), không đủ chỉ seed `ai_recommendations`.
</blockers>

<context>
Mọi thứ phía teacher flow của recommendations đã ổn định sau fixes. Gap thực tế hiện tại là luồng student không có điểm vào để mở `StudentRecommendationsTab` từ dashboard. Plan 05-05 đã mô tả chi tiết các file cần sửa (route_constants, app_router, student_home_content_screen, student_recommendations_tab, recommendation_providers). Khi resume, làm đúng theo plan này rồi quay lại verify-work để chốt UAT.
</context>

<next_action>
Start with: Mở `05-05-PLAN.md`, thực thi Task 1 trên `student_home_content_screen.dart` để thêm section "Hoc tap" + nút "Xem tat ca" điều hướng `context.pushNamed(AppRoute.studentRecommendationsTab)`.
</next_action>
