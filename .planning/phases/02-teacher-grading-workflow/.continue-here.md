---
phase: 02-teacher-grading-workflow
task: bug-testing
status: in_progress
last_updated: 2026-04-01T06:11:00Z
---

<current_state>
Vừa implement xong 3 tasks cho TeacherAssignmentDetailScreen + TeacherDistributeAssignmentScreen.
Test trên emulator-5556. CÒN LỖI cần fix phiên sau.
</current_state>

<completed_work>

- Task 1: Fix stat cards — đã implement nhưng CHƯA đúng (Sĩ số = 0 dù có học sinh)
- Task 2: Warning dialog "Sửa đề" + "Cấu hình" — ✅ HOẠT ĐỘNG (test OK)
- Task 3: Edit mode UI cho Cấu hình phân phối — ✅ UI đúng (lock bài tập, lock đối tượng, nút "Lưu cấu hình"), nhưng chưa test end-to-end PATCH
</completed_work>

<remaining_work>

### BUG 1 — Stat cards Sĩ số = 0
- Lớp "4" có 1 học sinh nhưng Sĩ số hiển thị 0
- Nghi ngờ: query class_members trong getDistributionDetail() dùng sai classId, hoặc RLS block
- File: lib/data/datasources/assignment_datasource.dart → getDistributionDetail()
- Fix: thêm AppLogger.debug log classId + members count, kiểm tra RLS policy class_members cho teacher

### BUG 2 — "Lưu cấu hình" chưa test end-to-end
- Chưa test bấm "Lưu cấu hình" → có PATCH lên Supabase không
- File: lib/presentation/providers/distribute_assignment_notifier.dart → updateDistribution()
- Fix: bấm lưu → xem log → verify DB update

### BUG 3 — Form prefill trống khi vào Edit mode
- Date/time fields trống sau khi navigate vào Cấu hình từ Detail
- Nghi ngờ: key names trong distributionConfig map khác với những gì loadDistributionConfig() parse
- Files: teacher_assignment_detail_screen.dart (build extra['distributionConfig']), distribute_assignment_notifier.dart (loadDistributionConfig)
- Fix: log distributionConfig map keys khi truyền qua → đối chiếu với loadDistributionConfig parsing
</remaining_work>

<decisions_made>

- updateDistribution() dùng PATCH không phải INSERT
- isEditMode=true + distributionId + distributionConfig truyền qua GoRouter extra
- loadDistributionConfig() gọi trong didChangeDependencies với flag _configLoaded
- _ClassRecipientAccordion nhận isEditMode → disable tap + lock icon thay nút xóa
- _validate() bị bypass trong updateDistribution() (đúng thiết kế)
</decisions_made>

<blockers>

- Sĩ số = 0: Cần kiểm tra RLS policy trên class_members cho teacher role
- Prefill trống: Cần log distributionConfig map keys để đối chiếu
</blockers>

<files_modified>

- lib/data/datasources/assignment_datasource.dart
- lib/data/repositories/assignment_repository_impl.dart
- lib/domain/repositories/assignment_repository.dart
- lib/presentation/providers/distribute_assignment_notifier.dart
- lib/presentation/views/assignment/teacher/teacher_distribute_assignment_screen.dart
- lib/presentation/views/class/teacher/teacher_assignment_detail_screen.dart
- lib/core/routes/app_router.dart
</files_modified>

<context>
Test account: giáo viên Hồ Ngọc Hà
Test class: lớp "4" (1 học sinh), bài tập "ai"
Emulator: emulator-5556 (700x1600)
ADB path: $LOCALAPPDATA/Android/Sdk/platform-tools/adb.exe
</context>

<next_action>
1. flutter run trên emulator-5556
2. Vào lớp "4" → bài tập "ai"
3. Kiểm tra log Sĩ số: thêm AppLogger.debug vào getDistributionDetail() log classId + members.length
4. Test "Lưu cấu hình": bấm nút → xem log updateDistribution → verify Supabase DB
5. Kiểm tra prefill: log distributionConfig map keys trước khi gọi loadDistributionConfig()
