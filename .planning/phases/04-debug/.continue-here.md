---
phase: 04-debug
task: 3
total_tasks: 8
status: paused
last_updated: 2026-03-23T04:41:16.480Z
---

<current_state>
77 analyzer errors → reduced to 3 errors. Working on fixing remaining errors systematically.
</current_state>

<completed_work>

- Task 1: Restore 4 missing/empty Phase 04 files - DONE
- Task 2: Fix ClassAnalytics entity + datasource + datasource output mapping - DONE
  - Added WorstOffender, SubjectDistribution Freezed classes to entity
  - Updated datasource to map SubjectDistribution with correct field names (subjectName, below50Count, below60Count, etc.)
  - Regenerated .freezed.dart and .g.dart
- Task 3: Fix analytics_providers.g.dart - DONE
  - TeacherStudentAnalyticsNotifier generated
  - analyticsEmptyStateProvider (parametrized Family) regenerated
  - teacherClassesForAnalytics + studentClassesForAnalytics generated
  - gradeOverrideHistoryProvider method added to teacher_submission_providers.dart
- Task 4: Fix scores_screen.dart - DONE (provider calls fixed)
- Task 5: Fix grade_distribution_heatmap.dart - DONE (imports fixed)
- Task 6: Fix teacher_analytics_screen.dart - DONE (ClassOverviewCard call fixed, MaterialPageRoute for TeacherStudentAnalyticsScreen navigation)
- Task 7: Fix teacher_student_analytics_screen.dart - DONE
- Task 8: Fix scores_screen unused imports - DONE
- Created `grade_audit_trail.dart` widget
- Fixed route_constants.dart (teacherStudentAnalytics route)
- Fixed classOverviewCard constructor (all params added)

</completed_work>

<remaining_work>

3 errors remain (analyzer errors):
1. `lateCount` getter undefined on `AssignmentDistribution` - `teacher_class_submission_list_screen.dart:143`
   - Fix: Add `lateCount` getter to AssignmentDistribution entity OR change usage to use existing field
   - **WORKAROUND: The existing entity uses `lateCount` parameter but it's a mismatch - distribution entity doesn't have it. The datasource extends fields from join queries. Need to add `lateCount` as an extended field in the entity, OR fix the usage to match existing entity fields.**
2. `gradeOverrideHistoryProvider` method undefined in `_TeacherSubmissionDetailScreenState` - `teacher_submission_detail_screen.dart:406`
   - Fix: Check if `gradeOverrideHistoryProvider` is properly defined in teacher_submission_providers.dart and .g.dart is regenerated
   - **NOTE: Method WAS added but placed inside `SubmissionGradingNotifier` class first, then moved out - g.dart may not have regenerated. Run `dart run build_runner build` after fixing method placement**
3. `onViewAnalytics` param undefined in `StudentClassDetailScreen` - `student_class_detail_screen.dart:52`
   - Fix: Add the parameter to the screen constructor or remove the call site
   - **Need to check what this param is supposed to do - likely a callback that was never implemented**
4. `totalSubmissions` required param mismatch in `ClassOverviewCard` - RESOLVED (was cache issue)
   - Note: After fixing, was showing as unresolved due to analyzer caching. Run `flutter pub get` + `dart run build_runner build` + `flutter analyze` fresh.

</remaining_work>

<decisions_made>

- Used Freezed for `SubjectDistribution` and `WorstOffender` classes (can't use plain Dart classes for Freezed serialization)
- Used `MaterialPageRoute` for `TeacherStudentAnalyticsScreen` navigation instead of GoRouter (simpler, no router changes needed)
- `gradeOverrideHistoryProvider` accidentally placed INSIDE `SubmissionGradingNotifier` class first, then moved out - needs g.dart regeneration
- `DesignIcons.md` doesn't exist - correct API is `mdSize` (existing code already has `DesignIcons.mdSize used correctly)
- Package imports `package:ai_mls/...` preferred over relative paths for analytics widget files (depth calculation errors)
- `analyticsEmptyStateProvider` and `studentAnalyticsNotifierProvider` are Family/parametrized providers (need `()` invocation with params)
- `scores_screen.dart` used non-existent DesignColors getters (background, surface, successLight, successDark) → replaced with existing tokens

</decisions_made>

<context>

**Root cause of all Phase 04 errors:** Phase 04 was COMPLETED but 4 files deleted before commit. When restored, cascading type mismatches exposed.
The dependency chain was: datasource calls ClassAnalytics() with fields → entity doesn't have fields → g.dart doesn't generate → screen references provider → broken.

**3 remaining errors are the LAST errors. All other 74+ errors fixed.**
Fix in order:
1. Run `dart run build_runner build` to regenerate g.dart for `gradeOverrideHistoryProvider`
2. Fix `lateCount` in `teacher_class_submission_list_screen.dart:143`
3. Fix `onViewAnalytics` in `student_class_detail_screen.dart:52`
4. Run `flutter analyze` to confirm 0 errors

</context>

<next_action>
Run `dart run build_runner build` first, then fix last 3 analyzer errors:
1. lateCount getter in teacher_class_submission_list_screen.dart → add to AssignmentDistribution entity or use existing field name
2. gradeOverrideHistoryProvider method → ensure it's top-level in teacher_submission_providers.dart
3. onViewAnalytics parameter → add to StudentClassDetailScreen constructor
Then verify `flutter analyze` shows 0 errors. If 0 errors, commit as Phase 04 complete.
</next_action>
