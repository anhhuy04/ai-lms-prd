---
phase: 01-student-assignment-workflow
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/views/assignment/assignment_list_screen.dart
  - lib/presentation/views/assignment/student/student_submission_confirm_screen.dart
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "User can access submission history from assignment list screen"
    - "User can access submission history after successful submission"
  artifacts:
    - path: "lib/presentation/views/assignment/assignment_list_screen.dart"
      provides: "Navigation link to submission history for students"
    - path: "lib/presentation/views/assignment/student/student_submission_confirm_screen.dart"
      provides: "Navigation link to submission history after submission"
  key_links:
    - from: "assignment_list_screen.dart"
      to: "student_submission_history_screen.dart"
      via: "goRouter"
      pattern: "goNamed.*studentSubmissionHistory"
---

<objective>
Add navigation links to access the existing submission history screen

The screen, provider, and route already exist. The gap is that users cannot navigate to the submission history screen. This plan adds navigation buttons to:
1. Assignment list screen (student view)
2. Submission confirm screen (after successful submission)
</objective>

<context>
@.planning/phases/01-student-assignment-workflow/01-UAT.md

# Existing Components (already implemented)
- Route: studentSubmissionHistory -> /student/submissions/history
- Provider: studentSubmissionHistoryProvider
- Screen: StudentSubmissionHistoryScreen

# Gap Analysis
- Test 7 expects: user can access the screen
- Current state: Screen exists but NO navigation links
- Fix: Add action buttons in Assignment List and Submission Confirm screens
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add history button to Assignment List Screen</name>
  <files>lib/presentation/views/assignment/assignment_list_screen.dart</files>
  <action>
    Add an IconButton to the AppBar actions in the student view section of AssignmentListScreen.

    Use this code pattern:
    appBar: AppBar(
      title: const Text('Danh sách bài tập'),
      backgroundColor: DesignColors.white,
      elevation: 0,
      actions: [
        if (userRole == 'student')
          IconButton(
            icon: const Icon(Icons.history),
            tooltip: 'Lịch sử nộp bài',
            onPressed: () => context.goNamed(AppRoute.studentSubmissionHistory),
          ),
      ],
    ),

    Note: Import AppRoute from route_constants.dart if not already imported.
  </action>
  <verify>
    <automated>flutter analyze lib/presentation/views/assignment/assignment_list_screen.dart 2>&1 | head -20</automated>
  </verify>
  <done>
    Student users see a history icon button in assignment list AppBar. Tapping navigates to submission history screen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add history button to Submission Confirm Screen</name>
  <files>lib/presentation/views/assignment/student/student_submission_confirm_screen.dart</files>
  <action>
    Add a secondary button below "Về trang chủ" that allows user to view submission history.

    Add import for go_router package if not already imported.
    Add a new TextButton after the "Về trang chủ" button:

    const SizedBox(height: DesignSpacing.md),

    // View history button
    TextButton(
      onPressed: () {
        context.goNamed(AppRoute.studentSubmissionHistory);
      },
      child: Text(
        'Xem lịch sử nộp bài',
        style: TextStyle(
          fontSize: 16,
          color: DesignColors.primary,
          fontWeight: FontWeight.w600,
        ),
      ),
    ),
  </action>
  <verify>
    <automated>flutter analyze lib/presentation/views/assignment/student/student_submission_confirm_screen.dart 2>&1 | head -20</automated>
  </verify>
  <done>
    After successful submission, user sees "Xem lịch sử nộp bài" button. Tapping navigates to submission history screen.
  </done>
</task>

</tasks>

<verification>
1. Run flutter analyze to ensure no errors
2. Verify both screens compile correctly
</verification>

<success_criteria>
- Assignment list screen shows history button for students
- Submission confirm screen shows "Xem lịch sử nộp bài" button
- Both buttons navigate to /student/submissions/history
- No analyzer errors
</success_criteria>

<output>
After completion, create .planning/phases/01-student-assignment-workflow/01-05-SUMMARY.md
