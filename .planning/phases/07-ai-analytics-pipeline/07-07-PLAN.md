---
phase: 07-ai-analytics-pipeline
plan: 07
type: execute
wave: 3
depends_on: [07-01]
files_modified: []
autonomous: false
requirements: [7-06]
must_haves:
  truths:
    - "Phase 4 UAT Test 1 (Radar Chart) shows skill mastery data from real MCQ submissions"
    - "Phase 4 UAT Test 4 (Strength/Weakness Card) shows data from skill mastery"
    - "Phase 4 UAT Tests 1b/8/9/11/12 (navigation fixes) pass on real device"
  artifacts: []
  key_links:
    - from: "student_skill_mastery trigger (07-01)"
      to: "analytics_datasource.dart READ queries"
      via: "Data populated by trigger, read by existing code"
      pattern: "student_skill_mastery"
---

<objective>
Close Phase 4 UAT debt by re-testing 7 skipped tests now that skill mastery data pipeline exists (from 07-01).

Purpose: Phase 4 analytics screens had no data to display. Now that 07-01 triggers populate skill_mastery, re-test to confirm screens show real data.
Output: All 7 Phase 4 UAT tests pass or bugs identified and fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-analytics-pipeline/07-CONTEXT.md
@.planning/phases/04-learning-analytics/04-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure test data exists — submit MCQ as student to trigger skill mastery population</name>
  <files></files>
  <read_first>
    - .planning/phases/07-ai-analytics-pipeline/07-CONTEXT.md (phase4_uat_debt section — the 7 tests)
    - .planning/phases/04-learning-analytics/04-UAT.md (if exists — original test descriptions)
    - lib/data/datasources/analytics_datasource.dart (lines 220-280 — how skill mastery is READ)
  </read_first>
  <action>
Before UAT re-test, ensure data pipeline is working:

1. **Verify triggers are deployed** (from 07-01 and 07-02):
   ```sql
   SELECT tgname FROM pg_trigger WHERE tgname LIKE 'trg_sa_%';
   ```
   Must return `trg_sa_01_skill_mastery` and `trg_sa_02_question_stats`.

2. **If no test data exists**, submit a test MCQ assignment as a student:
   - Login as test student in the app
   - Open an assigned MCQ assignment
   - Answer questions and submit
   - Verify data populated:
   ```sql
   SELECT * FROM student_skill_mastery WHERE student_id = '{test_student_id}' LIMIT 5;
   SELECT * FROM question_stats LIMIT 5;
   ```

3. **If triggers not deployed**, this plan is BLOCKED — go back to 07-01.

4. **Check analytics_datasource.dart** for any hardcoded filters or conditions that might prevent data display even when data exists. Fix if found.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - SQL query `SELECT * FROM student_skill_mastery` returns at least 1 row
    - SQL query `SELECT * FROM question_stats` returns at least 1 row
    - analytics_datasource.dart has no hardcoded filters blocking real data display
    - flutter analyze returns 0 errors
  </acceptance_criteria>
  <done>Test data exists in student_skill_mastery and question_stats. Analytics datasource reads it correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 4 UAT closure — data pipeline populates analytics tables, re-testing 7 skipped UAT tests</what-built>
  <how-to-verify>
**Group A: Data-dependent tests (unblocked by 07-01 triggers)**

Test 1 — Radar Chart:
1. Login as student who submitted MCQ
2. Open Analytics screen (Student Analytics Dashboard)
3. Verify Radar Chart shows skill mastery data (not empty)

Test 4 — Strength/Weakness Card:
1. On same Analytics screen
2. Verify Strength card shows top skills, Weakness card shows low skills
3. Data should come from student_skill_mastery

**Group B: Navigation tests (code fixed, need re-test)**

Test 1b Path C:
1. Go to Lop hoc (class list) -> select class -> Tien do hoc tap
2. Verify analytics screen loads with classId parameter

Test 8 — Teacher Class Overview Card:
1. Login as teacher -> Analytics -> select a class
2. Verify Class Overview Card shows aggregate data

Test 9 — Teacher Top/Bottom Performers:
1. On same Teacher Analytics class view
2. Verify top/bottom performer lists are populated

Test 11 — Teacher Navigation:
1. Teacher Dashboard -> tap analytics entry point
2. Teacher Class Detail -> tap analytics entry point
3. Both should navigate to teacher analytics

Test 12 — Shimmer Loading:
1. On TeacherAnalyticsScreen, observe loading state
2. Verify shimmer animation shows before data loads
  </how-to-verify>
  <resume-signal>Type "approved" with test results, or describe failures that need fixing</resume-signal>
</task>

</tasks>

<verification>
- student_skill_mastery has real data from MCQ submissions
- All 7 Phase 4 UAT tests re-tested and pass
</verification>

<success_criteria>
- Phase 4 UAT status changes from "partial" to "complete"
- All 7 skipped tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-analytics-pipeline/07-07-SUMMARY.md`
</output>
