---
phase: 07-ai-analytics-pipeline
plan: 05
type: execute
wave: 2
depends_on: [07-03]
files_modified:
  - lib/presentation/views/assignment/teacher/teacher_submission_list_screen.dart
  - lib/data/datasources/grade_override_datasource.dart
  - lib/presentation/views/assignment/teacher/widgets/submission/grading_action_buttons.dart
autonomous: false
requirements: [7-10]
must_haves:
  truths:
    - "Filter by Status chips (Cho doi cham, Nop muon) work correctly on teacher submission list"
    - "Grade Audit Trail displays override history when teacher has overridden scores"
    - "Override MCQ score works end-to-end: teacher overrides -> grade_overrides record created"
  artifacts:
    - path: "lib/presentation/views/assignment/teacher/teacher_submission_list_screen.dart"
      provides: "Fixed filter by status logic"
      contains: "filter"
    - path: "lib/presentation/views/assignment/teacher/widgets/submission/grading_action_buttons.dart"
      provides: "Grade override UI wiring verified"
      contains: "createGradeOverride"
  key_links:
    - from: "grading_action_buttons.dart override button"
      to: "grade_override_datasource.createGradeOverride()"
      via: "Provider call on button tap"
      pattern: "createGradeOverride"
---

<objective>
Close Phase 2 UAT debt: fix Filter by Status bug (D-23), re-test Grade Audit Trail and Override MCQ score (7-10b, 7-10c).

Purpose: Phase 2 UAT has 3 skipped tests that must pass before Phase 7 is considered complete.
Output: Fixed filter logic, verified grade override wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-analytics-pipeline/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix Filter by Status bug (7-10a, D-23)</name>
  <files>lib/presentation/views/assignment/teacher/teacher_submission_list_screen.dart</files>
  <read_first>
    - lib/presentation/views/assignment/teacher/teacher_submission_list_screen.dart (full file — find filter chip logic, understand how status filtering is implemented)
    - lib/data/datasources/submission_datasource.dart (if it exists — check if filtering is in datasource query or client-side)
    - .planning/phases/07-ai-analytics-pipeline/07-CONTEXT.md (D-23: investigate before fix)
  </read_first>
  <action>
Per D-23, investigate root cause BEFORE fixing:

1. **Investigation**: Read the submission list screen and find how filter chips work:
   - Are filter chips filtering client-side (already-fetched list) or server-side (query parameter)?
   - What status values are being compared? Are they matching DB values exactly (`submitted`, `graded`) or using different strings?
   - Check if `work_sessions.status` values match what the filter expects

2. **Likely bug patterns** (check each):
   - Filter chip uses `submissions.status` but data comes from `work_sessions.status` (different column)
   - Filter compares `SubmissionStatus.submitted` enum but DB string is `'submitted'` — case mismatch
   - "Cho doi cham" filter checks for `submitted` but late submissions also have `is_late=true` — filter misses the late flag
   - "Nop muon" filter checks `is_late` field but the field is not in the SELECT query

3. **Fix**: Based on investigation, fix the filter logic:
   - Ensure "Cho doi cham" (pending grading) filters for `work_sessions.status == 'submitted'`
   - Ensure "Nop muon" (late) filters for `is_late == true` from submissions table
   - If filtering is client-side, ensure the correct field is being read from the response data
   - If filtering is server-side, ensure the query parameter matches the DB column

4. **Add `AppLogger.info`** for filter state changes to aid debugging.
  </action>
  <verify>
    <automated>flutter analyze lib/presentation/views/assignment/teacher/teacher_submission_list_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - Investigation findings documented in code comments or commit message
    - Filter by Status "Cho doi cham" correctly shows only submissions with status == 'submitted'
    - Filter by Status "Nop muon" correctly shows only submissions where is_late == true
    - No hardcoded status strings that don't match DB values
    - flutter analyze returns 0 errors
  </acceptance_criteria>
  <done>Filter by Status chips work correctly. "Cho doi cham" and "Nop muon" filters show correct submissions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify grade override wiring and audit trail (7-10b, 7-10c)</name>
  <files>lib/presentation/views/assignment/teacher/widgets/submission/grading_action_buttons.dart, lib/presentation/views/assignment/teacher/teacher_submission_detail_screen.dart</files>
  <read_first>
    - lib/data/datasources/grade_override_datasource.dart (full file — understand createGradeOverride() signature and what it inserts)
    - lib/presentation/views/assignment/teacher/widgets/submission/grading_action_buttons.dart (find where override action is triggered)
    - lib/presentation/views/assignment/teacher/teacher_submission_detail_screen.dart (find where override history / audit trail is displayed)
    - db/schema_03_submissions_ai_analytics.sql (grade_overrides table definition lines 204-212)
  </read_first>
  <action>
Verify the full grade override chain works end-to-end:

1. **Verify grading_action_buttons.dart calls createGradeOverride()**:
   - Find the "Sua diem" / override button
   - Trace: button tap -> provider call -> repository -> datasource.createGradeOverride()
   - If the wiring is MISSING (button exists but doesn't call datasource), ADD the wiring:
     ```dart
     // On override confirm:
     await ref.read(gradeOverrideRepositoryProvider).createGradeOverride(
       submissionAnswerId: answerId,
       oldScore: currentScore,
       newScore: newScore,
       overriddenBy: teacherId,
       reason: reason,
     );
     ```

2. **Verify grade_overrides INSERT also updates submission_answers.final_score**:
   - Check if `createGradeOverride()` in datasource also UPDATEs `submission_answers.final_score`
   - If NOT, add the UPDATE:
     ```dart
     await _client.from('submission_answers')
         .update({'final_score': newScore})
         .eq('id', submissionAnswerId);
     ```
   - This UPDATE triggers the AFTER UPDATE trigger from 07-01 (trg_sa_update_recalc_mastery)

3. **Verify audit trail display**:
   - In teacher_submission_detail_screen.dart, find where override history is shown
   - Verify it calls `getOverrideHistory()` from datasource
   - If missing, add a section that lists override records with old_score, new_score, reason, overridden_by, created_at

4. **If any wiring is missing, add it**. If all wiring exists, document verification in commit message.
  </action>
  <verify>
    <automated>flutter analyze lib/presentation/views/assignment/teacher/widgets/submission/grading_action_buttons.dart lib/presentation/views/assignment/teacher/teacher_submission_detail_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - grading_action_buttons.dart contains call to `createGradeOverride` (directly or via provider)
    - grade_override_datasource.dart's createGradeOverride also updates submission_answers.final_score
    - teacher_submission_detail_screen.dart displays override history (calls getOverrideHistory or equivalent)
    - flutter analyze returns 0 errors
  </acceptance_criteria>
  <done>Grade override: teacher taps override -> grade_overrides INSERT + submission_answers.final_score UPDATE. Audit trail visible in detail screen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2 UAT closure: Filter by Status fix + Grade override wiring verification</what-built>
  <how-to-verify>
    1. Open app as Teacher
    2. Go to a submission list for an assignment with mixed statuses
    3. Tap "Cho doi cham" filter chip — verify only submitted (ungraded) submissions shown
    4. Tap "Nop muon" filter chip — verify only late submissions shown
    5. Open a graded MCQ submission, tap "Sua diem" (override)
    6. Change score and confirm — verify no errors
    7. Check grade_overrides table in Supabase dashboard — verify record exists with old_score and new_score
    8. Back to submission detail — verify override history section shows the override
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Filter by Status chips filter correctly
- Grade override creates grade_overrides record
- Override also updates submission_answers.final_score (triggers mastery recalculation)
- Audit trail displays in detail screen
- flutter analyze passes
</verification>

<success_criteria>
- Phase 2 UAT Tests 2, 7, 8 pass (Filter by Status, Grade Audit Trail, Override MCQ score)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-analytics-pipeline/07-05-SUMMARY.md`
</output>
