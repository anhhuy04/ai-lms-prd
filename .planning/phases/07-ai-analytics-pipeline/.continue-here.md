---
phase: 07-ai-analytics-pipeline
plan: bug-investigation
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-04-09T15:19:36.007Z
---

<current_state>
Wave 2 vừa hoàn thành (07-04, 07-05, 07-10 done + committed). Đang giữa Phase 1 của systematic debugging cho bug "AI review pipeline không hoạt động". Bị interrupt khi đang đọc 07-CONTEXT.md để tìm root cause.

Wave 2 status:
- 07-01, 07-02, 07-03 done (Wave 1 — done trước session này)
- 07-04 done (2c051a0) — Submission analytics pipeline + per-question timer
- 07-05 done (f231000) — Filter by Status fix + Grade Override/Audit Trail fix
- 07-10 done (5a6b2d1) — SubmissionStatus enum + AI toggle UI + settings safe-cast fix

Wave 3 chưa bắt đầu: 07-06, 07-07, 07-08

Bug đang điều tra (chưa tìm ra root cause):
User báo: AI toggle UI không hoạt động, ai_require_review không load từ DB, toàn bộ AI review flow bị broken.
</current_state>

<completed_work>
Session này hoàn thành:

07-04 (Wave 2):
- Task 1: Per-question Stopwatch — _questionTimers, _onQuestionChanged(), getTimeLog() trong workspace screen
- Task 2: _insertSubmissionAnalytics() — non-blocking, tính accuracy_by_tag, validate timeLog
- Thread timeLog qua: screen → WorkspaceNotifier → AssignmentRepository → Datasource
- SUMMARY.md created

07-10 (Wave 2):
- Task 1: SubmissionStatus enum + SubmissionStatusConverter (6dc18c2)
- Task 2: aiEnabled + requireReview trong DistributeAssignmentNotifier (4d07b52)
- Task 3: AI toggle UI AnimatedSize sub-section (5dbd390)
- Bug fix: Switch Transform.scale(0.65) + colors + settings safe-cast (5a6b2d1)
- SUMMARY.md created

Checkpoint 07-10: Human verify còn pending.
</completed_work>

<remaining_work>
1. FIX BUG (BLOCKING trước Wave 3)
   AI pipeline không hoạt động. Root cause hypothesis: submitAssignment() thiếu ai_processing branch + ai_queue INSERT. Đây có thể là scope của 07-08 (chưa run).

   Verify bằng:
   grep -n "ai_feedback_enabled\|ai_processing\|ai_queue" lib/data/datasources/assignment_datasource.dart

2. Wave 3 (sau bug fix)
   - 07-06: Edge Function process-ai-queue skeleton (Deno)
   - 07-07: Phase 4 UAT Closure
   - 07-08: Wire ai_queue INSERT into submitAssignment

3. Wave 4
   - 07-09: Phase 5 UAT + VERIFICATION gaps

4. Wave 5
   - 07-11: UI badges aiProcessing/pendingReview
</remaining_work>

<decisions_made>
- Per-question timer: GestureDetector.onTap thay vì PageController (workspace dùng ListView)
- timeLog: thread qua toàn bộ layer (Clean Architecture)
- Settings Map cast: Map.from(rawSettings) thay vì as Map<String,dynamic>?
- Switch sizing: Transform.scale(0.65) + custom colors match _buildToggleRow
</decisions_made>

<blockers>
- AI pipeline bug (BLOCKING): Chưa tìm root cause. Hypothesis: submitAssignment() thiếu ai_processing branch (scope 07-08 chưa chạy).
- 07-10 Checkpoint: Human verify pending.
</blockers>

<context>
Key spec từ 07-CONTEXT.md:
- D-11: submitAssignment() parse settings, nếu aiEnabled=true → INSERT feedback vào ai_queue cho tất cả MCQ
- D-12: Status transition: submitted→graded (AI tắt) HOẶC submitted→ai_processing (AI bật)
- D-17: Flutter Realtime subscription trên work_sessions khi ai_processing

Hiện tại submitAssignment() chỉ có path submitted→graded. Path ai_processing+ai_queue INSERT chưa có — đây là scope của 07-08 (Wave 3).

Khi user nói "AI review không hoạt động", có thể là feature chưa build hoặc bug thực sự.
</context>

<next_action>
Start with:
  grep -n "ai_feedback_enabled\|ai_processing\|ai_queue" lib/data/datasources/assignment_datasource.dart | head -20

Nếu KHÔNG có → ai_processing branch chưa implement → 07-08 scope, cần chạy plan đó.
Nếu CÓ nhưng broken → tìm root cause theo D-11/D-12 trong 07-CONTEXT.md.
</next_action>
