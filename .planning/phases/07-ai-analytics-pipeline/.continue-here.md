---
phase: 07-ai-analytics-pipeline
plan: wave-3-remaining
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-04-10T00:00:00.000Z
---

<current_state>
Wave 2 hoàn thành. Wave 3 đang tiến hành.

Bugs từ Wave 2 đã được fix (session 2026-04-10):
- Bug A: distributionDetailProvider invalidate sau updateDistribution()
- Bug B: submitAssignment() phân luồng status theo ai_feedback_enabled
- Bug C: Auto-publish guard + ai_feedback_enabled check trong teacher screen

Wave 3 đã hoàn thành (session 2026-04-10):
- 07-06 done: Edge Function process-ai-queue/index.ts (Deno, Gemini/Groq/Ollama)
  - feedback handler: MCQ explanation → submission_answers.ai_feedback (JSON 5 trường)
  - analysis handler: mastery < 0.6 → ai_recommendations INSERT
  - score handler: stub (essay deferred)
  - Fix: createClient<any> để tránh TS2345/TS2339 strict generic errors
- 07-08 done: submitAssignment() wire ai_queue INSERT
  - MCQ + aiEnabled → request_type='feedback' (mỗi câu)
  - Sau submit + aiEnabled → request_type='analysis' (kèm session_id trong payload)
</current_state>

<completed_work>
Bug fixes Wave 2:
- distribute_assignment_notifier.dart: ref.invalidate(distributionDetailProvider) sau updateDistribution()
- assignment_datasource.dart: submitStatus = aiEnabled ? 'submitted' : 'graded'
- teacher_submission_detail_screen.dart: _autoPublishFired guard + aiEnabled check

Wave 3 plans:
- supabase/functions/process-ai-queue/index.ts — CREATED
- assignment_datasource.dart — ai_queue feedback + analysis INSERT wired
</completed_work>

<remaining_work>
WAVE 3 CÒN LẠI:

1. TASK 6 — Nâng cấp UI render ai_feedback (PENDING)
   File: lib/presentation/views/assignment/teacher/teacher_submission_detail_screen.dart
   Method: _buildAiFeedbackBox()
   Việc cần làm:
   - Hiện tại đọc: (aiFeedbackRaw as Map)?['text'] — KHÔNG ĐÚNG với format mới
   - Edge Function ghi: {status, provider, model, is_correct, summary, explanation, misconception, tip, encouragement}
   - Cần render từng trường riêng biệt thay vì 1 khối text:
     * summary   → Text đậm, 1 dòng, màu success/error theo is_correct
     * explanation → Text body, italic
     * misconception → chỉ show nếu !is_correct, màu warning
     * tip → icon lightbulb + text
     * encouragement → text nhỏ, italic, cuối cùng
   - Fallback: nếu ai_feedback là String cũ → vẫn hiện như cũ

2. TASK: Trigger Edge Function sau submit (PENDING)
   Cách đơn giản nhất: Flutter gọi HTTP POST đến Edge Function sau khi INSERT ai_queue
   File: assignment_datasource.dart, sau đoạn analysis INSERT
   Hoặc: Supabase cron job (không cần sửa Flutter)

3. 07-07 — Phase 4 UAT Closure (PENDING)
   Verify skill_mastery có data từ DB trigger 07-01

4. 07-09 — Phase 5 UAT + VERIFICATION Closure (PENDING)

5. 07-11 — UI badges aiProcessing/pendingReview (PENDING, Wave 5)
</remaining_work>

<decisions_made>
- Edge Function: createClient<any> để bypass strict Supabase TS generics
- ai_feedback format: JSON 5 trường {summary, explanation, misconception, tip, encouragement}
- AI off → work_sessions.status = 'graded' ngay khi submit
- AI on → work_sessions.status = 'submitted' (chờ Edge Function)
- Auto-publish trong teacher screen: chỉ khi !aiEnabled && allObjective && !_autoPublishFired
- Edge Function trigger: cần quyết định (cron vs Flutter HTTP call vs pg_net)
</decisions_made>

<next_action>
Bắt đầu Task 6:
  File: lib/presentation/views/assignment/teacher/teacher_submission_detail_screen.dart
  Method: _buildAiFeedbackBox() — dòng ~897
  Đọc format ai_feedback từ Edge Function: {status, is_correct, summary, explanation, misconception, tip, encouragement}
  Render từng section riêng với visual phân biệt rõ đúng/sai
</next_action>
