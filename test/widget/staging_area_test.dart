// ignore_for_file: depend_on_referenced_packages, unused_import
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

// W0 stub — StagingAreaWidget will be created in Plan 07
// import 'package:ai_mls/presentation/views/assignment/teacher/widgets/staging_area_widget.dart';

void main() {
  group('StagingAreaWidget', () {
    group('Display', () {
      testWidgets(
        'renders list of QuestionDTO items',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
      testWidgets(
        'shows question text and answer choices for each item',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
      testWidgets(
        'shows empty state when questions list is empty',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
    });

    group('Actions', () {
      testWidgets(
        'Lưu vào Ngân hàng button calls saveToBank callback',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
      testWidgets(
        'Lưu và Thêm vào Đề thi button calls saveAndAdd callback',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
      testWidgets(
        'loading indicator shown while saving',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
    });

    group('DraggableScrollableSheet', () {
      testWidgets(
        'sheet starts at half screen height',
        (tester) async {
          markTestSkipped(
            'W0 stub — implement when StagingAreaWidget exists (Plan 07)',
          );
        },
      );
    });
  });
}
