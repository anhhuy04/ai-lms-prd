import 'package:flutter/material.dart';

/// Placeholder for Question Bank trash screen.
///
/// Real implementation comes in Task 5.8 of the Question Bank phase.
class TeacherQuestionTrashScreen extends StatelessWidget {
  const TeacherQuestionTrashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Trash — coming in Task 5.8')),
    );
  }
}
