import 'package:flutter/material.dart';

/// Placeholder for Question Bank list screen.
///
/// Real implementation comes in Task 5.4 of the Question Bank phase.
class TeacherQuestionBankScreen extends StatelessWidget {
  const TeacherQuestionBankScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Question Bank — coming in Task 5.4')),
    );
  }
}
